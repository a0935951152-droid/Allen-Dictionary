# 規格（按管線順序）

> 精確到演算法步驟、可追溯 code（`services/api/app/engine/`）。天條/鐵律見 [CLAUDE.md](CLAUDE.md)。
>
> 管線塊：**① 知識側建置 · ② 切分 · ③ 接地 · ④ 偵測 · ⑤ 判官(gemma 四態) · ⑥ 人工/回灌**（知識側＋判定側兩層）。
> 執行前提見文末「全 GPU、無降級備案」。

---

# ① 知識側規格（純淨資料 → 心智圖 + CAG）

模組 `knowledge.py`。目標：從**比待校稿乾淨的來源**自動長出正解知識庫；**只進不錯的詞**。
端點 `POST /api/knowledge/build`（`dic build`）。

## 1.1 收什麼（來源准入）

**鐵律**：知識側只收 authored 乾淨源；逐字稿/字幕/ASR 校正含錯字 → 污染知識側，一律排除。

兩道守門（`_auto_decision`）：
| 守門 | 規則 | 常數 |
|---|---|---|
| 副檔白名單 | 只收 `.xlsx .docx .pptx .pdf` | `_CLEAN_EXT` |
| 檔名黑名單 | 含 `字幕\|逐字\|校正\|三語對齊\|transcript\|L3` → 排除 | `_DIRTY_NAME` |

- 乾淨副檔 **且** 檔名非髒 → `extract`；否則 `exclude`。
- **人工逐檔覆寫**：`.decisions.json`（`rel → extract|exclude`），凌駕自動判斷（`set_decision`/`_effective_decision`）；
  `extract` 可強收被排除的逐字檔、`exclude` 可排除乾淨檔。
- **封存**：被排除檔可移 `sources/_excluded/`（`os.walk` 跳過 `_`/`.` 開頭子目錄）。
- **增量**：內容 hash manifest（`_file_hash`/`.manifest.json`），未變檔讀快取不重抽。

## 1.2 用什麼收（兩種來源 × 兩種抽法）

```
sources/ ─┬─ .xlsx 對照表 ──→ 規則解析（無 LLM）──────────────→ T1 gold
          └─ .docx/.pptx/.pdf ─→ 文字抽取 → 切 chunk(700字) → Gemma 兩階段 ─→ T2/T3
```

### (a) 對照表 xlsx（glossary）＝ gold — 規則解析，不過 LLM
`_read_correction_xlsx`，見 §1.4。

### (b) 文件 docx/pptx/pdf — Gemma 兩階段抽取（`_extract_doc`；獨佔窗口）
1. 文字抽取：`_read_docx/_read_pptx/_read_pdf`。
2. 切 chunk：`_chunk_text(size=700)`。
3. **Pass1 譯前語意分析**（`_pass1_semantic` → `_pass1_prompt`，vLLM Gemma（獨佔窗口，served-name=judge））：
   - 抽 `entities[{name, type(8類), meaning(語境真意), 多語固定譯法 en/ja/…}]` + `relationships[{source,target,desc}]`（GraphRAG 邊）。
   - **gleaning 補抽**：把已抽詞回灌「只補遺漏」，重跑至無新詞（預設 1 趟）。
   - 守門：`name in chunk` 且過 `_is_term`；多語名過 `_clean_name`（按書寫系統限長度/詞數）。
   - 容錯：逐物件解析 `_iter_objs`（要求扁平 JSON，不巢狀）。
4. **Pass2 特殊詞 CAG**（`_pass2_special` → `_pass2_prompt`）：
   - 吃 Pass1 全局關鍵詞脈絡 + chunk，以 **A/B/C 可操作判準**挑「值得進術語表」的詞 → `is_special=True`。
   - **A** 專有名稱（人/地/組織/產品/品牌/作品）｜**B** 具名概念（微笑曲線/王道/二八法則）｜**C** 同音易誤辨專詞。
   - **鐵則**：受過教育的譯者免查就能譯對的一般詞（主管/行動/毛利/培訓）一律不收（寧缺勿濫）。
5. **Tune**（`_tune_categories`）：心智圖出來後，對落不到 8 類的詞回判類別（few-shot + 守門 + 未分類兜底）。

## 1.3 怎麼驗證（守門 + 證據分層 + 保護）

**詞守門**（`_is_term`）：`≤16 字`、無中/英句讀(`_SENT`/`_ASCII_SENT`)、非純數字(`_PURE_NUM`)、非英文填充語(`_LATIN_FILLER`)。
**譯法守門**（`_clean_name`）：按書寫系統(`langtag.name_limits`)限長度/詞數，擋整句（過長/句末標點/內嵌引號）。

**證據分層 tier**（`_harvest_sources` line 678-687；termhood + 翻譯關鍵性 + ASR 風險，取代主觀「coined」）：
| tier | 來源 | is_special | confidence | 去向 |
|---|---|---|---|---|
| **T1** | 對照表（有 ASR 變體） | ✅ | 0.8+（高/中/低=0.9/0.7/0.5） | CAG + 聲學索引 |
| **T1g** | 對照表（無變體） | ✅ | 同上 | CAG |
| **T2** | LLM Pass2 標特殊 | ✅ | 0.7 | CAG 候選 |
| **T3** | LLM 純語意（Pass1 only） | ❌ | 0.5 | **僅心智圖**（不進 CAG） |

- **CAG = is_special 子集**（T1/T1g/T2）；T3 純語意排除（`_write_cag`/`_write_acoustic` 只收 is_special）。
- **gold 保護**（`_aggregate`）：glossary 詞的 term/變體/類別/信心**不被 LLM 覆寫**；只補空語意欄（gloss/names/關係）——
  第一原則（語意接地）貫穿對照表詞，但只補空不污染人工資料。非 gold 才併 LLM 變體。
- **locked 保護**（§11）：人工核可 `locked:true` → 重建保留 `_LOCKED_FIELDS`(term/category/gloss/variants/reason/names)。
- **verified 閘門**（治理鐵律）：只有 **T1 gold + 人工核可** 驅動校正/升 gold；LLM 抽的(T2/T3)＝candidate，只供偵測/導航。

## 1.4 對照表格式（xlsx）

**模糊表頭比對**（`_col`：表頭含關鍵字即對映，避開計數欄＝含「數」字）。多工作表，每頁找到「正解欄」才當對照表（否則跳過＝說明/總覽頁）。

| 欄位 | 表頭關鍵字 | 必要 | 處理 |
|---|---|---|---|
| **正解** | 正確 / 建議修正 / 正名 | ✅ | 過 `_is_term`；以 `flatten` 為合併 key |
| **錯字（變體）** | 錯字 / 辨識 / 誤辨 | — | 用 `、,，;；/空白` 切多個；各過 `_is_term`、去重、排除等於正解 |
| **類型** | 類型 / 分類 | — | 取前 16 字；屬 `_SKIP_CAT`(全句修正/日語校正差異)→跳過該列 |
| **信心** | 信心 | — | 高=0.9 中=0.7 低=0.5（`_CONF`），缺→0.8 |
| **上下文** | 上下文 / 範例 / 彙總 | — | 取前 80 字 |

- 同正解（flatten key）跨列/跨頁**合併變體**，信心取 max。
- 產出列：`{term, variants[], category, confidence, source:"glossary:<檔名>", context}` → tier 標 T1/T1g、is_special=True。
- 現用對照表：`觀光署STT_錯字與專有名詞字典.xlsx`、`AIspeakin_專有名詞修正對照彙整…xlsx`、`標題分類總表…xlsx`。

## 1.5 分類 taxonomy（8 類實體）

`_TAXO`：人名 / 組織機構 / 地點地名 / 產品品牌 / 技術或概念術語 / 外來詞或英文詞 / 事件活動 / 作品或標題；
兜底 `_UNCAT=未分類`。`_canon_category` 把雜類併回 8 類。

## 1.6 產出（落地）

| 產物 | 檔 | 內容 | 函式 |
|---|---|---|---|
| 心智圖 | `vault/terms/*.md`（274） | frontmatter(term/type/gloss/tier/is_special/locked/names/variants/ipa) + `[[關係/同音/錯法/領域]]` | `_write_vault` |
| 領域節點 | `vault/domains/*.md`（8+1） | 8 類 + 未分類 | `_write_vault` |
| CAG 詞表 | `cag/<domain>.md` | is_special 子集（canonical/variants/兩面向） | `_write_cag` |
| 聲學種子 | `acoustic.json`（274/變體 312） | `{correct, variants, is_special, tier}` → 餵 IPA 索引 | `_write_acoustic` |

> 心智圖 ⊃ CAG（is_special）：一份實體兩投影——心智圖給語意（接地檢索）、CAG 給正確度（判官候選+聲學靶）。

---

# ② 切分規格（逐字稿 → core chunk）

模組 `chunk.py`（核心 `make_offset_chunks`）+ `review.make_segments_offsets`。端點 `POST /api/batches/{b}/segment`。
目標：把逐字稿切成**判定單位 chunk**；core 連續鋪滿不漏、不重疊（重疊只會重複污染判定訊號）。

## 2.1 輸入 / 前處理

- 輸入：`batch.raw.text`（逐字稿 .txt/.srt；srt 先去時間軸 `_read_srt`）。
- **多語逐字稿前處理**：SpeakIn 式 `zh:/en:/ja:` + `--- N` 編號單元。
  - 有 `--- N` 標記（`_has_markers`≥2）→ `parse_units`：按編號單元切，`zh:` 行為中文、`en:/ja:` 存 `ref`（同段對照，供多通道一致性）。
  - 純中文 → `extract_zh`（去 en/ja 行與標記）→ `split_sentences`（句末 `。！？!?；;\n` 後切）。

## 2.2 演算法（offset-aware + bge 邊界 + core/context）

1. **atom 化**（`_atoms`）：句/編號單元＝最小不可分 atom，組 `zh_full`（保留標點全文，atom 間以 `\n` 接、offset 連續）；
   單 atom 超 400 字 → `_hard_split` 硬切。每 atom 帶 `{text, ref, start, end}`。
2. **bge 主題邊界**（`make_offset_chunks` + `_pack_valley`）：
   - `vecs = clients.embed([atom.text…])`（bge-m3）→ 相鄰相似度 `sims[i]=cos(vec_i, vec_{i+1})`。
   - 貪婪打包：先吃到 `lo`，再在 `[lo,hi]` 字數窗內**切在相似度低谷**（`sims` 最小＝主題轉折）。
   - **降級**：embed 不可用 → `sims=None` → 規則貪婪（盡量靠近 `hi`）。
3. **core 連續鋪滿**：`core_text = zh_full[start:end]`，`end` 延伸到**下一塊起點**（吃掉 atom 間 `\n`），最後一塊到全文尾 → 無縫無重疊覆蓋。
4. **下游單位**：`zh = strip_punct(core_text)`（去所有標點/空白＝純實字，下游收斂/偵測吃這個）。
5. **context（只顯示）**：`ctx_before/after = zh_full[start-ov : start]` / `[end : end+ov]`（重疊 padding，只供切分站手動校正視圖、**不往下游流**）。

## 2.3 參數 / 產出

| 參數 | 值 | 說明 |
|---|---|---|
| `hi` | 400 | core 字數上限 |
| `lo` | `max(120, hi//2)`=200 | core 字數下限 |
| `ov` | 40 | context 重疊 padding 字數 |

產出 `Segment[]`（`seg_id=c_000…`）：`{start, end, core_text, raw_text=zh(去標點), normalized, ref, ctx_before, ctx_after}`。
**重切會清空 `b.spans`**（seg_id 全失效，避免孤兒 span）。
> 規則版 `make_chunks`/`chunk_text`（無 bge）保留作降級/相容。

---

# ③ 接地規格（chunk → 語境錨點，不改字）

模組 `know_index.py`（心智圖端）+ `wiki_index.py`（wiki 大庫）；端點 `POST /api/batches/{b}/ground_context`。
**翻轉核心**：先立語境錨點再修。**心智圖是 bge 橋**——chunk→相關心智圖實體（乾淨語境）→以實體當 query 查 wiki，
wiki 結果**依乾淨語境 grounded**（不是 raw chunk 直查，避免 coined 詞噪音 AI→醫生遊戲）。

## 3.1 前置建置（一次性）

- **wiki 大庫**（`wiki_index.build`，`dic wiki`）：`entries.jsonl`（title｜aliases）→ `clients.embed`(bge-m3, 批次 128)
  → **hnswlib**（cosine, dim 1024, ~1.5M, 增量 `add_items`）→ `data/index/wiki/{hnsw.bin,ids.jsonl,meta.json}`。
- **心智圖嵌入**（`know_index.build`，`dic mind`）：每 vault 實體 `_entity_text = term｜gloss｜names｜variants`
  → bge 向量 → `cards.json`（增量：term 內容 hash，只嵌新/變動）。

## 3.2 runtime 演算法（每 chunk）

1. **心智圖檢索**（`know_index.retrieve(text, k=6, min_score=mind_floor)`）：
   `clients.embed([chunk core])` → 對 cards 向量暴力**餘弦**（`ground._cosine`，274 小不需 hnsw）→ topk 且 `score≥mind_floor`
   → 相關心智圖實體（乾淨語境），`{source:"mind", term, gloss, category, variants, is_special, score}`。
2. **wiki 橋接**（每個 mind 實體當 query）：
   `q = term + "｜" + gloss` → `wiki_index.search(q, k=wiki_per)`（embed q → hnsw knn → `score=1-cos距離`）
   → 過 `score≥wiki_floor`、去重 title → `{source:"wiki", via:<橋接實體>, title, aliases, score}`。
3. **寫錨點**：`seg.ground_context = mind_cards + wiki_cards`（兩源；**不改字**）。

## 3.3 參數 / 產出 / 治理

| 參數（`ground_context_ep`） | 值 | 作用 |
|---|---|---|
| `k` | 6 | 每 chunk 取幾個心智圖實體 |
| `mind_floor` | 0.35 | chunk→心智圖 餘弦下限 |
| `wiki_per` | 2 | 每實體取幾個 wiki |
| `wiki_floor` | 0.55（前端 graph 0.65） | 實體→wiki 相似下限 |

產出 `seg.ground_context[]`（mind/wiki 兩源錨點），當下游**判官的語境證據**（§⑤：判官看兩軸＋錨點＋多語拍板）。
**治理**：wiki 一律 candidate、永不升 gold（鐵律 5）；接地**不改字**，只立語境。降級：缺 hnswlib/embed → 回空（不崩）。
> 前端：`graph.html` 點實體即時 `/api/wiki/search`、「🔗 wiki 接地圖」`/api/wiki/links` 視覺化此橋接。

---

# ④ 偵測規格（chunk → 可疑 span）

模組 `review.review_detect_segment`；端點 `POST /api/batches/{b}/review`（或由 `coarse_fix` 內部呼叫）。
目標：圈出**像專名的可疑 span** + 對 CAG 撈音近正解候選（detect→retrieve）。**NER＝可錯定位器**，認對交 ⑤⑥。

## 4.1 雙偵測器

- **A 變體掃描**（`phonetic_index.scan_variants`）：在 chunk 直接字串掃**已登錄 CAG 變體**（人工核可）→ 確定誤聽，必抓（不靠 NER）。
- **B NER + IPA**（`clients.ner` ckip → `phonetic_index.query`）：
  1. NER 抓專名實體 span（BIOES 完整實體）；過濾 `_SKIP_TYPES`(數字/日期/量值)、`len≥2`、`w in chunk`。
  2. `is_target(w)` → 跳過（w 本身已是 CAG 正解，不該改）。
  3. `query(w)`：對 CAG 過 **IPA 逐字距離**（CAG `_IPA_FUZZY≤0.28` / 字典·refterms `_IPA_EXACT≈0`）撈音近正解；無→不硬改。

## 4.2 產出 span

每命中產 `SpanNode`：`history=[原詞, 建議正解]`（`by=model`）、`current=建議正解`、`review=存疑`、`state=unknown`、
`grounding.candidates=[正解…]`、`is_proper_noun=True`、`category=NER 類型`。

> ⑤⑥ 才拍板：偵測階段一律『存疑』，不直接改字（寧缺勿濫）。

---

# ⑤ 判官規格（gemma 四態，取代舊「粗修+精修」）

模組 `refine.judge_span`/`judge_many`；端點 `POST /api/batches/{b}/coarse_fix`（單批）、`POST /api/judge_pending`（跨批，
搭 `dic judge` Gemma 獨佔窗口）。對象：`review==存疑` 的 span。需先 `/ground_context`（已算 `reports`/`rule_hit`）。

## 5.1 兩軸並行（規則閘，ground_context 已算）

`reports.build`：**IPA命中軸**（`acoustic`：登錄變體必收 score=1.0 ＋ IPA→CAG 音近召回）× **局部語意撐軸**
（`semantic_local`：候選 vs 局部句，經心智圖實體→wiki 橋的 anchM∪anchW，排名制前 k=5≥0.35）。
兩軸**獨立量測**（非先後）；`rule_hit`＝兩軸皆撐。語意**只對音召回的候選表態、不生成候選字**（防語意造詞）。

## 5.2 判官四態（`judge_span`）

1. 候選＝`grounding.candidates[0]`；無 → 維持。`local`＝局部句；`parallel`＝平行多語。
2. **gemma prompt**：原詞/候選/這句/聲學渲染/局部語意命中/平行多語 → `{"abandon":bool,"xling":bool}`（max_tokens=32, temp=0）。
   `abandon`＝原詞在這句講得通就維持；`xling`＝平行多語支持改成候選。
3. **四態** = `rule_hit` × `abandon`（若 `¬rule_hit ∧ ¬xling` 強制 `abandon=True`）：
   - ① `rule_hit ∧ ¬abandon` → **修**（`decision.to=auto, correct=候選, refined=True, review=錯字`）。
   - ②③ 命中+棄 / 不命中+棄 → **維持**（`review=正確, decision.to=pending`，不進字典）。
   - ④ `¬rule_hit ∧ ¬abandon`（跨語救回）→ **人工**（`decision.to=human, correct=建議`，永不自動）。

## 5.3 治理

- 併發 `JUDGE_CONCURRENCY=4`；`temperature=0`。**候選只取讀音召回**（CAG/心智圖 curated）；wiki 只當語境、永不升 gold。
- 不自由改寫（防自創：宏雞→KFC），對齊 abstain/copy-input（2405.15216/2505.17410）。

---

# ⑥ 人工 / 回灌規格

## 6.1 人工清單（端點 `POST /api/batches/{b}/refine`）

列出判官判為第④態（`decision.to=human`）的 span，附建議候選＋上下文，供人工抽屜逐一裁決。不自動改字。

## 6.2 回灌（`commit` + `dictionary/sync`）

- `commit`：只併 `decision.to==auto 且 decision.correct` 的 **verified** → `dictionary.json`（`correct`+`current` 當變體、信心↑）。
- `sync`：重生三套 → **落檔 `data/correct/sync.json`**：②regex（安全子集，可盲替換）＋contextual（通用詞/字典詞變體，不得盲替換）；①IPA(g2pW)/③TTS(CosyVoice) pending。
- **聲學閉環（不經 acoustic.json）**：字典本身是 `phonetic_index._build` 三源之一——每個 correct 直接成「真同音層」聲學靶，mtime 監看 commit 後自動重建（下批更準）。acoustic.json 只由知識側 build 寫（天條8 佈局分離）；升 CAG 走 /cag.html 人工核可。
- **治理閘門**：只併 verified（對照表 gold + 人工核可）；candidate(T2/T3/wiki)只供偵測/導航，不自動升 gold。

---

## ⚙ 執行前提（2026-06-24 方向：全 GPU、無降級備案）

> **使用者定**：切塊/接地等**一律過模型**，移除「模型不可用時的 CPU 降級」（原 bge→規則貪婪、hnswlib/embed→回空、
> 缺套件→no-op 等 best-effort fallback）。模型服務缺 → **報錯中止**，不靜默降級。
> 例外：**IPA 為規則式聲學表徵（MISA 天條①），刻意純 CPU，非降級備案 → 保留**。
> ⚠ 此方向與 CLAUDE.md 天條③「降級不壞」衝突，待同步調整天條與移除 code fallback（見回報問題）。
