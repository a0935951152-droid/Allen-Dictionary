# 收斂式字典建立架構〔已拆分 · 僅留歷史對照〕

> ⚠️ **2026-06-24 重構**：本單檔已拆分為 [CLAUDE.md](CLAUDE.md)（天條/索引）、
> [系統架構.md](系統架構.md)、[資料結構與資料庫.md](資料結構與資料庫.md)、[功能與API.md](功能與API.md)。
> 內容以新文件為準（含 wiki 大庫接地翻轉、單層 IPA 等），本檔僅留作歷史對照、不再維護。

> **一句話**：從「比待校稿乾淨的來源」自動長出**正解知識庫**（知識側），再用它去**糾正錯誤逐字稿**（收斂側）；
> 收斂不靠 ground truth，靠「多版本是否收斂」＋「能否接地」當信心訊號，收穫回灌字典 → sync 三套系統 → 下一輪底子更高（閉環）。
>
> **狀態**：收斂引擎＋接地＋分流＋字典＋sync 已實作；知識側（兩階段抽取 + CAG + 心智圖後端 + 增量擴充）已接線。
> 最後更新：2026-06-23（知識側：分類軸改 8 類實體 taxonomy＋未分類＋tune；只收 authored 乾淨源；
> **多語抽取落實**＝langtag.LANGS 語言註冊表（規格(一) 26 語系）+ names 扁平抽取，取代寫死 en/ja；
> glossary 語意接地貫穿＝只補空不覆寫）。前次 2026-06-22：重構全文三部分明、§3 站1.5 診斷＋τ 旋鈕、增量可擴充。

---

## 0. 核心理念

| 理念 | 內容 |
|------|------|
| **兩側一環** | **知識側**（上游，建正解知識庫）→ **收斂側**（下游，糾正稿）→ 收穫回灌字典 → sync → 下一輪（環） |
| 收斂為主軸 | 不靠正解，靠「多版本/多輪是否收斂」判斷哪個字可疑 |
| 雙偵測器 | **收斂**抓搖擺、**接地**抓「穩定地錯」；兩者缺一不可（見下） |
| 可視化為前提 | 每個節點都可在前端瀏覽/編輯，JSON-backed、API 可達、可腳本 |
| 三類分流 | 穩定+接地→建議收錄；搖擺(龍夢升騰)→人工；穩定但錯(宏雞)→產 regex |
| 人工逐輪 | 收斂不自動連跑：人工按一次跑一輪、生成新資料再修改，N/M/K 漸成形 |
| **治理鐵律** | LLM 抽的一律 `candidate`；**只有 verified（對照表 gold + 人工核可）驅動校正/進聲學索引/進乾淨 CAG**。人類擁有最終字典 |

### 0.1 兩個失敗模式 → 兩個偵測器

```
收斂?   接地通過?   判定                    出口
穩定     ✅          可信                    建議收錄
穩定     ❌(宏雞)    系統性錯字              產 regex 給人工
搖擺     —(龍夢升騰) 模型沒把握              人工裁決
```

> 關鍵：**「穩定」≠「對」**。常用詞穩定可信；專有名詞就算穩定，也要過一次接地，
> 否則宏雞這種「穩定地錯」會大搖大擺過關。2026 跨模型一致性文獻自承「agreement 高 ≠ 正確」
> （模型會一致地錯），故第二偵測器（接地）是**必要**而非補丁。

### 0.2 與 2026 SOTA 對照（本架構＝組裝多個已驗證技術）

| 架構假設 | 2026 文獻 | 結論 |
|---|---|---|
| 收斂/多版本當信心訊號 | 多通道一致性以 agreement/disagreement 量化信心；跨模型分歧偵測 | ✅ 驗證 |
| 分層裁決：便宜先跑、貴的留最後 | Multi-stage：先測不可靠句、只對它跑 LLM；ReASC 省 70–80% 成本 | ✅ 強驗證 |
| 停止條件 N/M/K | 熵感知推論、ReASC 高信心早停/模糊加權 | ✅ 套用 ReASC |
| wiki/Wikidata 接地、不微調 | ELERAG：Wikidata entity linking + RRF 重排 | ✅ 驗證 + RRF |
| 收斂靠 embedding 相似度 | ELERAG 警告：純語意相似度不保證事實正確 | ⚠️ 補 entity linking + 音韻通道 |
| 事前只知領域 → 預熱 | 廣泛描述當弱語意先驗；hotword 檢索 top-k 偏置 | ✅ 驗證 |
| biasing 把詞餵進 ASR | 大詞表下檢索 compact top-k 再偏置，而非全塞 | ✅ 精修為 top-k |
| 離線 wiki dump（不打線上） | 「Helpful Context Leaks」證實上下文偏置會洩漏內容 | ✅ 升級為必要 |
| 知識預載、不每次檢索 | CAG（Cache-Augmented Generation）三階段 | ✅ 知識側 CAG |
| 知識庫＝實體+關係+補抽 | GraphRAG（類型 taxonomy + gleaning + 關係） | ✅ 兩階段抽取 |

---

## 1. 全景：兩側資料流

```
┌──────────────────────── 知識側（純淨側，上游，§5）────────────────────────┐
│  data/knowledge/sources/  乾淨來源(xlsx/docx/pptx/pdf；排逐字·字幕·校正,可逐檔覆寫) │
│        │ build（增量：content-hash 只重抽改動檔；鎖定欄位不覆寫）          │
│        ▼                                                                   │
│   兩階段 LLM 抽取（Pass1 語意 → Pass2 術語）                                │
│        ├─▶ vault/   一詞一 .md（心智圖；全部實體）                         │
│        ├─▶ cag/     判官隨身小抄（is_special 子集，高信任）                │
│        └─▶ index/acoustic/  音韻索引種子（term+variants 回灌）            │
└───────────────────────────────┬───────────────────────────────────────────┘
                                 │  餵 ↓（CAG 常駐前綴 / 接地音韻通道 / top-k 偏置）
┌──────────────────────── 收斂側（糾正側，下游，§2–4）──────────────────────┐
│  錯誤逐字稿 → 切分(bge·core/context) → 審查→循環收斂 → 接地與分流          │
└───────────────────────────────┬───────────────────────────────────────────┘
                                 ▼
┌──────────────────────── 匯流回灌（閉環，§4.3）────────────────────────────┐
│  字典（去重/累積 variant/領域滾動/信心↑）→ 同步回灌 ①IPA ②regex ③TTS       │
│        └─▶ 回灌：高信任 verified 詞 → 知識側 CAG/聲學；下一輪底子更高        │
└────────────────────────────────────────────────────────────────────────────┘
```

### 1.1 收斂管線（功能分階，詳細流程）

每階段「操控鈕＋視覺化＋前端可改」；以**功能**命名（不用站號，前端 tab 同名）。
全管線 coarse-to-fine 的 **retrieval-then-correct**（先接地建語境錨點、再由粗到細修；採 Apple 2409.15353）：
**先用 wiki 大庫＋心智圖建語境錨點 → 粗修明顯同音錯 → Qwen3.8-27B Q2_K_L 帶 CAG/心智圖精修一定要正確的字**。
便宜檢索（bge/hnswlib/音韻）早做且廣、貴的 LLM 驗證晚做且窄、只對仍模糊的 span。
> **2026-06-24 翻轉**：原為「CAG 小庫精修在前、接地兜底在後」；改為「**先接地後精修**」——
> 在還不知整段在講什麼時就改字會改錯既成事實，故先立語境錨點再修。

```
匯入 ─▶ 切分 ─▶ 接地(語境錨點) ─▶ 粗修 ─▶ 精修 ─▶ (循環收斂) ─▶ 分流 ─▶ 字典 ─▶ 同步回灌 ─╮
  ▲                                                                                       │ 回灌
  ╰───────────────────────────────────────────────────────────────────────────────────────╯
```

**每階段：輸入 → 機制 → 輸出**

1. **逐字稿匯入** — 輸入：STT/TTT 逐字稿（**僅 .txt/.srt**；乾淨文件走知識庫，§1.2 鐵律）。
   機制：存 raw + 不覆寫 v0 原稿；舊模型 智慧整理日期/來源（可手調）。輸出：`batch.raw` + v0 + meta。

2. **切分**（offset-aware + bge 邊界 + core/context；§2.1 詳） — 輸入：raw。
   機制：抽中文→句/編號單元 atom →**bge 把 core 切點對齊主題轉折**（相鄰 atom 相似度低谷；缺服務則規則貪婪降級），
   **core 連續鋪滿全文（無縫、不重疊）＝覆蓋優先不漏內容**，目標 200~400 字。
   每塊存 `[start,end]` offset、`core_text`、去標點 `zh`（下游收斂單位）、`ctx_before/after`（只供手動校正視圖）。
   輸出：`segments[]`。診斷(regime §2.2)為可選旁支，不擋主流程。

3. **接地（語境錨點；§4.1；先跑、不改字）** — 輸入：core chunk。
   機制：**心智圖實體當 bge query 橋** → 檢索 wiki 大庫（hnswlib，標題+redirect，~1.5M）＋心智圖卡 →
   每 chunk 撈相關實體＝「這段在講哪些實體」。離線先把**心智圖→wiki 連結豐化**（`know_index`，§5.x）。
   輸出：`seg.ground_context`（廣度語料 candidate，**永不升 gold**）。
   〔狀態：wiki 索引 `wiki_index`（hnswlib）、`know_index`、`ground_context` 端點已實作；建置中〕

4. **粗修（明顯同音錯；§4.1 步驟②）** — 輸入：chunk + 語境錨點。
   機制：**偵測（CPU 雙偵測器）** A 變體直接掃描（CAG 變體必抓）＋ B ner（BIOES 完整實體，可錯定位器）→
   對 **CAG** 撈音近正解（層1 拼音 key → 層2 IPA ≤0.28）→ **錨點高信心支持者直接改**、其餘留精修（`coarse`）。
   輸出：粗修 span（`coarse=auto`）＋ 存疑 span。

5. **精修（LLM 受限驗證；GPU 舊小模型；§3.0 step c）** — 輸入：仍存疑的 span。
   機制：judge 帶 span 上下文 ＋ **語境錨點** ＋ **CAG/心智圖候選** → 「選候選/保持原樣（棄權，RPM）」受限選擇，
   **不自由改寫**。候選正解只取 **CAG/心智圖（curated）**，wiki 只當語境。〔狀態：已接線〕

6. **循環收斂**（可選深化，ReASC 多輪；§3.1/3.3/3.5；需 GPU） — 輸入：精修仍模糊的 span。
   機制：舊模型 約束式重生（溫度 0.4）→ 詞級對齊 → 同音閘門 → 分歧自浮 → N/M/K 停止；壞輪可退回快照。

7. **分流**（§4.2） — `auto`（粗修/穩定常用詞，直接收）/`grounded`（接到地）/`regex|human`（接不到地）。

8. **字典 + 同步回灌**（§4.3） — 去重/累積 variant/信心↑（**只併 verified**）；
   重生 IPA/regex/TTS 三套；正解＋變體回灌 §5.6 `acoustic.json` → 音韻索引（閉環，下批更準）。

### 1.2 資料佈局（純淨側 vs 糾正側，鐵律）

| 路徑 | 側 | 內容 | 鐵律 |
|---|---|---|---|
| `data/knowledge/` | **純淨側** | sources/(原檔) → vault/(心智圖) + cag/(判官前綴) + index/acoustic 種子 | **只收 authored 乾淨來源**（`.xlsx/.docx/.pptx/.pdf`）；**排除逐字稿/字幕/ASR 校正**（`.srt/.txt` 或檔名含 字幕/逐字/校正/三語對齊/transcript/L3），否則錯字被當正解、污染知識側 |
| `data/index/` | **糾正側** | chunks/(切分產出) + acoustic/(聲學庫) | query 側、含錯字，勿與純淨側混 |

> `files/refterms`＝聲學檢索的參考正解詞表（前身 `files/ANE`，已更名）。各目錄規格見其 README。

---

# Part A — 收斂側（糾正逐字稿）

## 2. 切分與診斷

### 2.1 切分（chunk）為收斂單位（RAG 式切分）

raw 逐字稿（會議後／事前用 STT/TTT 轉成）先切成片段 **chunk** 再跑收斂——**整檔一次跑收斂太慢**。
切法採**便宜規則切（RAG 式遞迴邊界，無 LLM/embedding）**：

- 多語逐字稿按 **"--- N" 單元邊界**、純中文按**句界(。！？)** 為切點，貪婪打包到長度上限（~400 字）。
- **不重疊**：在句界切，詞不被切斷，故不需重疊（重疊只會讓相鄰塊重複同句、雙重污染收斂訊號）。
- 存下的 chunk **去掉所有標點與空白**（純實字）——標點/空格是後續對齊的噪音，會害收斂亂切；
  原文用字一律保留（**不整段套 OpenCC**，s2t 會把已正確的繁體 岩→巖、里→裏 改錯）。
- **語意切分（可選升級）**：用既有 bge-m3 嵌入（TEI 離線）算相鄰句相似度，在主題跳變處下刀。
- en/ja 對照存入 `Segment.ref`（多通道交叉驗證）。

chunk 間獨立、可並行（§7 樂觀並發）；各 chunk 結果按原序「重組」回整份。
**順序：先切好 chunk，再做審查/收斂**（切分是便宜前處理，與引擎無關）。實測野柳 138k 字 → 62 chunk。

### 2.2 診斷：先診斷再收斂（regime profiling；可選旁支）

**收斂分兩步：先「診斷這批稿是什麼問題」，再用對應強度收斂。** 不同講者問題**正交、解法不同**：

| 講稿類型 | 武器 | 用錯會怎樣 |
|---|---|---|
| 乾淨但詞彙特殊（正確率高、專名多） | **接地 + 字典偏置**（wiki 全開、hotword top-k） | 狂跑收斂 → 沒分歧、空轉 |
| 雜訊大但通用（錯誤多、沒特殊詞） | **收斂**（samples↑、輪數↑、τ 嚴） | 狂查 wiki → 都常用詞、查無、浪費判官 |

> 文獻定位＝**reference-free ASR 品質估計**（NoRefER / GT-free WER）＋**training-free difficulty proxy**
> （從生成過程本身讀難度，據此分配 test-time 算力）。關鍵：不只看靜態統計，而看這批稿**對旋鈕的「反應」**（如分歧數對溫度的斜率）。

**探針**（在抽樣 5–10 個 chunk 上跑，重用既有 generate/align/NER、無新模型）：

| # | 探針（旋鈕） | 量什麼 | 指向軸 |
|---|---|---|---|
| 0 | 錯誤密度 | 浮出的同音分歧每 100 字數量——**錯誤量本身** | noise（主） |
| 1 | 溫度敏感度 | 分歧數對溫度 {0.2,0.7} 的斜率（陡＝不穩） | noise |
| 2 | 同音閘門殺除率 | 原始分歧中「非同音（被閘門丟）」佔比（高＝一直改寫＝稿亂） | noise |
| 3 | 收斂 margin | top-1 群質量平均（self-consistency 信心；低＝雜訊） | noise |
| 4 | NER 密度 × 信心 | 實體多且平均信心低 → OOV/專特 | rarity |
| 5 | 字典 OOV 率（+ 接地命中，待 wiki dump） | 抽樣專名不在累積字典 → 新/特殊領域 | rarity |

> **noise ＝錯誤量(主) + 收斂不穩(輔)**：刻意把「錯誤多但好修」與「錯誤少但模糊」分開——前者多丟人工沒意義，後者才需嚴 τ／多輪。
> 0–3 聚成 noise、4–5 聚成 rarity → 2D 座標 → 四象限自動配 preset（可覆寫）：

| 象限 | preset |
|---|---|
| 乾淨×通用（easy） | τ 寬、samples 1、M 1–2、接地抽查 → 近乎全自動 |
| 乾淨×專特 | 收斂輕，重心切到**接地全開 + top-k 偏置**；τ 中 |
| 雜訊×通用 | **收斂主戰場**：samples↑、M↑、τ 嚴；接地需求低 |
| 雜訊×專特（hard） | 全開最嚴：samples↑↑、M↑↑、τ 嚴、接地全開＋偏置、多丟人工 |

**必處理的混淆**：第 5 號「接地沒命中」同時是 rarity 高與 noise 高的症狀。用既有**同音收斂消歧**解耦：
候選**同音穩定**但查無 → 新/罕見**真詞**（rarity）→ 進字典提案；候選**搖擺**且查無 → **錯誤/雜訊**（noise）→ 收斂/regex。

**輸出**：整批座標 + 逐 chunk 座標 `chunks:[{seg_id,noise,rarity,regime}]` + 白話描述 `descriptions`（規則式、無 LLM）。
存上 `BatchNode.profile`（§6）；循環收斂的 τ／samples／M 預設由 preset 帶入，仍保留「人工逐輪」可手調。
**前端**：併入「切分」階段的可選旁支——象限散佈圖（每 chunk 一點、依 regime 上色、點可進該 chunk；◆＝整批位置）＋描述清單。

---

## 3. 審查與循環收斂（收斂引擎）

**§3 兩階段**：①**審查（主線，§3.0）＝偵測→召回→LLM 受限驗證**（切分後的下一關，便宜召回 CPU + LLM 驗證 GPU）；
②**循環收斂（§3.1 起，可選深化）＝對審查仍模糊的 span 跑多輪生成分歧**。先審查、需要才收斂。

### 3.0 審查主線：偵測 → 召回 → LLM 受限驗證（detect-retrieve-verify）

採 Apple 2409.15353 的**召回→驗證骨架**：便宜的檢索（CPU）早做且廣、貴的 LLM 驗證（GPU）晚做且窄、只對可疑 span。

**① 偵測（純 CPU，雙偵測器）** — 圈出可疑 span（可錯，認對交後面）：
- **A 變體直接掃描**（不靠 ner）：文本含已登錄 CAG 變體字串（宏基→宏碁）→ 人工核可的**確定誤聽，必抓**。
- **B ner ＋ 定位**：ckip NER 抓實體當「可錯定位器」，補 A 沒登錄的新音近錯字。
  ＊ckip 是 **BIOES**（B/I/E/S，非 BIO）；HF `aggregation="simple"` 只懂 BIO、把 `E-` 誤判成新類型 → 碎片化（宏/碁分家）。
  解法：ner 服務 `aggregation="none"` 取原始逐字標籤，自寫 **BIOES 精解析**（`B`/`E` 切邊界 → 相鄰同類正確切分：台積電∣宏碁）。
  NER＝**可錯的定位器非辨識器**（Apple：「偵測與正解不符時正是檢索發揮處」），不負責認對。

**② 召回（純 CPU，候選全來自 CAG）** — 對每個 span 撈音近正解候選：
- **層1 離散拼音 key** 完全比對 → **層2 IPA 音素加權編輯距離**（≤0.28；`ipa.py`，撈未登錄音近）。
- **聲學靶嚴格只收 CAG（is_special）**，T3 一般詞不當靶（避免過度修正往無關詞）；`is_target` 本身為正解 → 跳過、免改。
- 落地 `index/acoustic/ipa_index.json`（§5.6）。候選≤10、按距離排，當提示不直接換。

**③ LLM 受限驗證（GPU，舊小模型）** — 對有候選的 span 做最終決定：
- judge 帶 **span 上下文（語意）＋ CAG 候選清單**，做「**選某候選 / 保持原樣（棄權）**」的受限選擇。
- **不自由改寫**（否則回到 舊模型 自創坑：宏雞→KFC）；對齊「abstain/copy-input 防 over-correction」（2405.15216、2505.17410）。

**程序級流程（函數調用鏈，`review.review_detect_segment`）**：
```
偵測（純 CPU）
  variant_hits = phonetic_index.scan_variants(text)   # CAG 變體字串直接掃描 → [{surface,correct,start}]
  ents         = clients.ner(text)                     # ner 服務 BIOES 精解析 → 完整實體 [{word,type,start,end}]
逐 span 召回（hybrid 兩路；候選全來自 CAG）
  if phonetic_index.is_target(w): continue             # w 本身已是正解 → 跳過、免改
  讀音路(CPU)  phon = phonetic_index.query(w)          # 層1 pinyin_key 完全比對 → 層2 ipa.distance ≤ 0.28
  語意路(GPU)  vecs = clients.embed([ctx] + CAG詞)     # bge-m3 bi-encoder
              sem  = topk_i(_cosine(ctx_vec, cag_vec_i))  # 上下文 × 每個 CAG 詞 餘弦，取前 k
  融合         cands = _rrf(rank(phon), rank(sem))[:10] # RRF 混兩路排名（見下）
驗證（GPU qwen，待接）
  judge_verify(span.context, cands)                    # Qwen3.8-27B Q2_K_L 受限選擇：選某候選 / 保持原樣；RPM 信心<0.5 棄權
```

**hybrid 怎麼混（RRF，`ground._rrf`）**：兩路量綱不同（讀音是 IPA 距離、語意是餘弦），無法直接加權。RRF **只看名次、不看絕對分**：
```
_rrf(*rankings, k=60):                  # 每個 ranking＝一路「按相關度排好的候選 list」
  score = {}
  for ranked in rankings:
    for rank, cand in enumerate(ranked):
      score[cand] += 1 / (k + rank)     # 名次越前加越多；任一路靠前就升總分
  return sorted(score, 由高到低)
```
語意路撈「上下文相關」、讀音路撈「同音無關」（紅雞↔宏碁語意撈不到、只讀音撈到）——**任一路撈到就進池**，RRF 把兩邊名次融成單一排序。

**現行檢索：bge-m3 嵌入＋心智圖/wiki 錨點（cross-encoder reranker 已移除）**：
- **bge（bi-encoder，`clients.embed`）**：query 與候選**各自**編碼成向量算餘弦。快（候選向量可預建），精度普通 → 用於**召回**（從庫撈一批）。
- 現行沒有 cross-encoder 精排；局部候選由 bge-m3 排名，最後交 Qwen 受限判定。
- 現行兩段 = **bge-m3 召回／語境錨點 → Qwen 受限判定**；CAG 的音近候選另由 IPA 軸召回。

**為何 IPA 不直接換、要 LLM 拍板（文獻實證防誤修）**：Apple 2409.15353 對照——直接用 IPA top-1 替換（simple replacement）WER **4.84**，反比 context-aware LLM 決定（**4.44**）**差**。故 IPA 只召回、LLM 帶上下文受限選擇、可棄權。

**數據來源 / 職責（§1.2 兩側撮合）**：

| 元件 | 來源 | 側 |
|---|---|---|
| span 位置（哪裡可疑） | 偵測器（變體掃描＋ner）跑在逐字稿 | 糾正側 |
| 候選詞（可能正解） | **CAG**（變體／拼音 key／IPA） | 純淨側 |
| 主語意（這句講什麼） | span 所在 **chunk 上下文** | 糾正側 |
| 輔助語意（候選背景） | 心智圖／CAG 詞間關係（可選） | 純淨側 |
| 最終決定（改/不改/選哪個） | LLM 帶上述撮合 | — |

> 純淨側給「答案候選」、糾正側給「問題語境」，LLM 撮合。數據已備於 `SpanNode`：`context`（主語意）＋ `grounding.candidates`（候選），LLM 驗證直接讀、不再撈。

**context 保持小**：只給 span 局部窗口 ＋ CAG 候選（幾十~幾百字），非整篇稿/整個心智圖。長 context 的 prefill 計算密集（GPU 強項，不該丟 CPU）；本任務無需塞大。
**模型選型**：受限驗證＝有棄權的選擇題。先用現有 **舊小模型**（GPU，受限選擇比自由改寫約束強，零成本驗證管線）。升級候選 **舊 8B 候選**（2501.13921，function calling 強制 enum 受限輸出，從機制杜絕自創；3B/8B、Llama 3.2 基礎、繁中強化）。

### 3.1 候選靠生成分歧自己浮出（循環收斂階段；不靠預選）

> 此為**循環收斂**（審查 §3.0 仍模糊的 span 才跑的多輪深化；需 GPU）。與審查共用詞級對齊／同音閘門。

對齊 §0.2 第一條（多版本 agreement/disagreement＝信心主信號）：

```
每輪（審查＝第1輪、循環收斂＝第 n 輪）對每個 chunk：
  1. 舊模型 約束式重生一版（temperature≈0.4；只改『確定』的同音/形近 ASR 錯字，禁同義/虛詞/翻譯）
  2. 先斷詞（重用本地 ckip/NER），把 [raw, v1, v2, …] 做【詞級對齊】（不做字元級）
  3. 同一詞位上版本間有分歧 → 浮成異常 span；【span 帶上下文】（詞 + 周圍視窗，存 SpanNode.context），
     身分以「詞位置」為鍵跨輪穩定（一直變＝可疑、各版本一致＝穩定不列）
  4. 【同音閘門】分歧候選彼此須同音/近音才算 ASR 錯字；非同音（還有↔以及）＝同義改寫/雜訊，丟棄
  5. 收斂(state)+接地導出判定 正確/存疑/錯字（非 LLM 主觀拍板）
```

> **為何詞級＋帶上下文（取代字元級）**：字元級 diff 只留變動的幾個字，切出無上下文碎片（`經`／`資`）——人機都無從判斷，
> 且邊界逐輪飄移使 span 身分每輪改變、歷史接不起來而難收斂。對齊單位改「詞」、span 帶上下文，才對齊 §6 `SpanNode.context`
> 與「diff 出『錯→對+上下文』」之意圖（EMNLP 2025 NEC：實體級＋語音檢索定位）。
> **同音閘門**：ASR 錯字本質是同音/近音替換；self-consistency 對「系統性同義偏差」（模型固定把 還有→以及）無效——
> 投票越多越「穩定地錯」，故須以同音性把同義改寫擋在浮成 span 之前。
> **NER＝可錯的「定位器」非「辨識器」（採 Apple 2409.15353）**：ckip NER 只負責**圈出**像專名的 span（標 is_proper_noun/category），
> **不負責認對**——認對交 §4.1 聲學檢索。Apple 金句：「偵測輸出與正解不符時，正是檢索發揮作用之處」（偵測可錯、內容靠音近檢索修）。
> 本架構的「分歧自浮」比 Apple 的單次 inline 標籤更強（多版本不一致 > 單次偵測），故 ckip 在此僅輔助標類別，主偵測信號＝生成分歧。

### 3.2 生成 wiki-free + 約束式最小修正

> 若每輪都讓 LLM 接 wiki 自由改寫，會把本來對的字也一直「改進」→**製造假搖擺、污染收斂訊號**。
> 所以：生成多版本時 wiki-free，wiki/接地只當**一次性驗證關卡**。

- **護欄**：生成冒出外文或長度暴走即**退回原文**；詞級對齊後再以**同音閘門**濾掉非同音的同義/虛詞改寫。
- **NER（本地 ckip）只標專名/類別**，不當候選來源（通用 NER 對領域詞涵蓋有限）。
- **多通道(en/ja)對照**：切分時保留 en/ja 為 `Segment.ref`，餵 舊模型 當交叉驗證（en『Queen's Head』→『女王頭』）。
- **異體字前置**：岩/巖 這類純字形變體，先用 OpenCC 變體表攤平，不進收斂迴圈（0 成本）。

### 3.3 分層裁決（便宜先跑、貴的留最後；ReASC 自適應）

```
每個 span：
1. NER + 信心：是專有名詞/低信心嗎？
     ├─ 否（常用詞）─────────▶ 不碰，免 wiki 免 LLM           ← 篩掉約 9 成
     └─ 是 → 收斂（多版本比對，ReASC 自適應）
              • 高信心（早就一致）→ 早停                       ← 省 70–80% 算力
              • 模糊 → 多跑幾輪加權聚合
              ├─ 穩定 + 接地✅ ─▶ 建議收錄（接地只查這一次）
              ├─ 穩定 + 接地❌ ─▶ 【宏雞】產 regex 給人工
              └─ 搖擺（跨輪一直變）─▶ 【龍夢升騰】人工裁決
```

### 3.4 收斂信心閘 τ：穩定、可預測嚴格度的旋鈕（risk–coverage 校準）

把散落的旋鈕（溫度、同音閘門、reject 護欄、N/M/K）收成**一個接受嚴格度 τ**；溫度固定 0.4 當「探索量」，τ 當「接受量」。

```
每個 span 一輪末：
1. 等價分群 E（★音韻收斂，非 NLI 語意）：E(a,b)= flatten 相同 OR 同音（重用 variants.flatten + homophonic）
2. 群質量 p_i = 群大小/K；收斂分數 s = margin(=max p_i) ×(1−λ·正規化熵)，專名再乘接地分數
3. 單一 τ 決策（取代 N/M/K 硬判）：
     s ≥ τ_high → auto（建議收錄；專名過接地）
     s ≤ τ_low  → human（龍夢升騰）
     之間        → 再跑一輪（ReASC 自適應，上限 M）
```

> **為何可預測**：掃 τ 即 **risk–coverage 曲線**——自動收錄比例(coverage)與自動錯誤率(risk)單調對應；
> 反過來可說「自動錯誤率 ≤ α」，用 **conformal/選擇性預測**在小校準集上解出 τ，τ 不再玄學。
> **為何配接地**：純語意熵不足以安全棄答（「Entropy Alone is Insufficient」），故 s 必含接地——正好＝雙偵測器。

### 3.5 停止條件 N/M/K（ReASC 框架）

| 結果 | 條件 | 出口 |
|------|------|------|
| 穩定 | 連續 N 輪不變（或一開始就高信心早停） | 接地後建議收錄 |
| 搖擺 | 跑滿 M 輪仍在 K 個寫法間跳 | 人工裁決 |
| 卡死 | 穩定但接地失敗 | 產 regex |

> N/M/K 決定「自動 vs 丟人工」比例，由使用者實機自測。**先當上色/提示用閾值**，透過「逐輪」流程慢慢成形，
> 成熟後才交引擎自動早停。

### 3.6 互動：人工按一次跑一輪（手動逐輪）

```
人工按一次 ─▶ 引擎跑「一輪」：生成新版本 + 重算收斂/分歧
          ─▶ 前端把每個 span 上色（綠/黃/紅）當「建議」，不自動停
          ─▶ 人工檢視、必要時直接改某些 span
          ─▶ 再按一次 → 下一輪（上一輪人工修改餵進這輪生成）
```

- **人工編輯不算 volatility**：`history` 每筆標 `by: model|human`，搖擺偵測只計 model 變動，避免把「人工改字」誤判為「模型不穩」。
- **分類注入**：跑 r 前手選一筆**具名 LLM 提示詞**（描述領域分類依據，如「地質：判斷是否為地形/岩體/海岸地名…」）
  注入為 `domain`/`domain_prior`（弱先驗）與審查提示詞前綴。分類由使用者前端維護（增刪改）。
- **循環退回**：每輪前對 spans 快照；某輪跑歪可一鍵**退回**上一快照，重選分類/參數再跑，不必重建批次。
- **排序規則**：**不穩（volatile）與「比對不到」（專名未接地）排最上、穩定沉最下**（人只碰黃跟紅）。

---

## 4. 接地、分流、匯流

### 4.1 接地節點：entity linking + RRF + 音韻檢索通道（採 ELERAG 2026）

> **2026-06-24 翻轉（管線重排，§1.1）**：接地由「最後兜底」提到「**最前面、先跑、不改字**」——
> 先用 **wiki 大庫（標題+redirect，bge-m3→hnswlib，~1.5M，`wiki_index`）＋心智圖橋（`know_index`）**
> 對每個 chunk 撈**語境錨點**（`seg.ground_context`），再粗修/精修。wiki 一律 candidate、永不升 gold。
> 心智圖→wiki 為**離線連結豐化**（每實體連到最相關 wiki 條目存成語境卡）。建置：
> `wiki_ingest`(解析 zhwiki dump) → `wiki_index`(嵌入) → `POST /knowledge/link_wiki`(連結) → `POST .../ground_context`。
>
> 下文的 per-span `ground.ground_span`（bge 語意×IPA 兩軸）保留為**接地驗證/OOV 兜底**（後段），
> 大庫版讀音降為**否決關卡**（語意像但讀音差太遠→drop）；門檻拍板邏輯不變。

> 純語意相似度不保證事實正確。驗證時把「embedding 相似」與「wiki/Wikidata entity 命中」用
> **Reciprocal Rank Fusion** 融合再排序，取信心最高的標準寫法。豆腐巖→豆腐岩走這條；宏雞查無有效實體→判 stuck。

**音韻檢索（聲學 RAG，第三條 RRF 通道）**：ASR 錯字「語意壞、讀音沒壞」→ 純語意檢索會在最難的同音錯字上失靈
（`豆腐沿` 語意上不像 `豆腐岩`）。故 `ground` 加一條**按讀音檢索**通道（`phonetic_index`）：
對正解側（累積字典 + 參考詞表 + 知識側 CAG 種子）建**兩層聲學索引**（層1 拼音 key、層2 IPA 音素距離，§5.6），
用候選讀音撈回同音/近音正解，與 bge 語意錨點做**兩軸判讀**，再由門檻/LLM 拍板（音韻只負責召回）。
聲學正規化採規則式 IPA（純 CPU、零訓練；非學習式嵌入——後者與天條衝突，見 §5.6）。
**語意服務不可用時音韻仍可離線接地。**

**第二次 hybrid 接地（大庫兜底，`ground.ground_span` 大庫版）程序級流程**：
審查（§3.0）用 CAG 小庫精修「不能錯的詞」；**CAG 未命中的專名/OOV 才進此關**，用大庫（wiki dump + 外部字典）兜底。
**走名詞語意、不走讀音召回**（讀音降為否決關卡）：
```
ground_span(span, big_db):                          # GPU
  召回(bge)    sem = bge_recall(span.context, big_db)   # bi-encoder 從大庫撈一批候選（快、粗）
  讀音否決      rr  = [c for c in rr if ipa.distance(span.surface, c) ≤ 否決門檻]
                # 守門員：語意像但讀音差太遠 → drop（豆腐沿→豆腐岩 留、→豆腐腦 否決）
  語意錨點      anchors = semantic_local(span.context, sem)
  門檻拍板      g.rrf_score = normalize(fused[best])
                g.pass_ = g.rrf_score ≥ grounding_threshold   # 低於 → 不接、保持原樣/人工（寧缺勿濫）
  (可選)Qwen3.8-27B Q2_K_L 驗證  judge_confirm(span.context, best)        # 上下文確認「真是這實體嗎」，可棄權
```
**防接錯五層 ↔ 程序對映**：①門檻(`g.pass_`) ②兩軸一致（IPA 候選需局部語意撐） ③讀音否決(`ipa.distance` 守門)
④Qwen3.8-27B Q2_K_L 驗證(`judge_confirm`) ⑤大庫候選按乾淨度降權(召回時加權)。

**審查 ↔ 接地差異**：審查候選＝CAG（讀音為召回主力、語意輔助）；接地候選＝大庫（語意為召回主力、讀音降為否決）。
`ground.py` 現有 RRF 三路框架（語意 `_cosine` × `reranker（已移除）` × 音韻 `phonetic_index.query`）即此雛形——大庫版把音韻從「召回路」改「否決關卡」、候選源從 CAG 換成大庫向量庫。

### 4.2 分流（三類出口）

```
穩定+接地✅ → 建議收錄    穩定+接地❌(宏雞) → 產 regex 給人工    搖擺(龍夢升騰) → 人工裁決
```

可疑 span 一個個浮成節點顯示前端，你看著它們在圖上收斂——穩定沉下去（綠）、搖擺一直跳（黃）、卡死鎖在錯值（紅）。人只碰黃跟紅。

### 4.3 字典與同步回灌（字典 → 三套系統 → 閉環）

字典彙整節點的優化邏輯（**最終收錄由使用者親自判斷**；下列為輔助提案，非全自動入庫）：

1. **去重**：同 `correct` 一條 `TermEntry`，多批累積進 `occurrences[]`。
2. **累積 variant**：各批 stuck/correction 的 `wrong` → `variants[]` → 餵 ② regex、回灌知識側聲學種子。
3. **領域滾動**：依 `domains[]` 聚成可重用領域包 → 餵下一場「來源/預熱」。
4. **信心提升**：越多批佐證 `confidence` 越高 → 高信心自動 sync，低信心待人工。
5. **sync**：重生三套 → ① IPA 音標、② regex 規則、③ 偏置改**檢索 top-k**（非全塞）。

```
這批收斂收穫 ─commit─▶ 字典彙整 ─sync─▶ ①IPA ②regex ③TTS ─回灌─▶ 下一批/下一場
                                              └─ verified 高信任詞 → 知識側 CAG/聲學種子
```

---

# Part B — 知識側（建知識庫）

## 5. 知識側建構（CAG 詞表 + Obsidian 心智圖 + 聲學種子）

收斂側（Part A）糾正稿；**知識側**從「比待校稿乾淨的來源」自動長出**正解知識庫**，餵 §4.1 音韻檢索與判官 CAG 常駐。
實作 `engine/knowledge.py`，**CAG 只由後端 `POST /api/knowledge/build` 改**。

### 5.1 資料結構的真實意義（來源 → 用途，誰消費）

| 結構 | 是什麼 | 來源 | 用途 |
|---|---|---|---|
| `sources/` | 原檔堆 | **你手動丟**（唯一人工點；支援子目錄遞迴，可 `sources/<講者>/` 分場） | 進料口，被 `build` 解析 |
| `vault/terms/*.md` | 一個正解名詞 + gloss + 變體 + 多語 names + 關係 | 對照表(gold) 或文件 GraphRAG 抽取 | 知識庫最小單元；組心智圖、供 CAG/聲學取用 |
| `variants`（詞內欄位） | 已知 **ASR 錯法**（豆腐沿…） | 對照表的「錯字」欄 | **聲學索引同音種子**（§4.1 按讀音撈正解）、產 regex |
| `tier`（T1/T1g/T2/T3） | 信任度標籤 | 證據：對照表 gold / 跨檔多次 / 單次 | **CAG 門檻**——決定誰進判官小抄 |
| 關係邊 `[[X]]（desc）` | 兩實體間語意關係 | GraphRAG `source→target` | 心智圖**語意連線**（非僅同音/領域） |
| `vault/domains/*.md` | 類別彙整 | 8 類實體 taxonomy＋未分類 分組 | 心智圖領域節點 |
| **`cag/<domain>.md`** | **判官隨身小抄**＝高信任正解詞表（依類別分組） | **T1+T1g+T2 的詞**（對照表 gold + LLM 跨檔多次；排除 T3 單次） | **預載進判官 context（固定 prompt 前綴，靠 llama.cpp prefix-cache 常駐 KV）→ 校正時「腦中一直記著正確領域詞」，免每句檢索** |
| `index/acoustic/*.json` | 音韻索引種子（未接線） | 知識側 term+variants 回灌 | §4.1 接地按讀音檢索正解 |

> **CAG 一句話**：來源＝知識側篩出的高信任詞（有證據才收）；用途＝判官的**常駐前綴記憶**。
> 與 §4.1 聲學檢索互補：**CAG 記住常見核心、聲學檢索補罕見長尾**（Hybrid CAG-RAG）。
> **CAG 變吵＝好事**：CAG 偏召回（漏掉施振榮的「王道/微笑曲線」比多收幾個雜詞糟得多，代價不對稱）；
> 多收的雜詞本身無害校正——除非聲學上像某正確詞又被選中，而那一步由接地+收斂把關。分工：**CAG 撒大網（高召回）→ 接地做精度閘**。

### 5.2 抽取：兩階段 coarse-to-fine（一份實體、兩投影；CAG ⊂ 心智圖）

xlsx 對照表＝**gold（T1）**直接採；docx/pptx/pdf **乾淨文件**走**兩階段 LLM**（Pass2 吃 Pass1 全局理解 → CAG 更準）。
**知識側只收 authored 乾淨源**：`_CLEAN_EXT={.xlsx/.docx/.pptx/.pdf}` ＋ `_DIRTY_NAME=(字幕|逐字|校正|三語對齊|transcript|L3)` 兩道守門，逐字稿/字幕/ASR 校正一律排除（含 ASR 錯字、會污染知識側）。

```
Pass 1 語意心智圖（廣度/召回）：整份乾淨文件一次輸入 → term·type(8類實體)·gloss·多語names·關係
        ↓ 聚合文件級「已理解實體清單」＝全局脈絡
Pass 2 術語表收錄（精度/不能錯）：吃 Pass1 全局 + 全文 → 以 termhood 判準挑收錄詞
        判準 A 專有名稱 / B 具名概念(需固定譯法) / C 同音易誤；鐵則「譯者不需查就會譯的詞不收」
        ↓
Tune  心智圖成形後 → `_tune_categories` 對著整張圖重判「未分類/落不到 8 類」的詞（便宜、只動 misfit）
        ↓
心智圖＝全部實體（Pass1）      CAG＝is_special 子集（Pass2 A/B/C + 對照表），B 概念詞優先區
```

- **第一原則＝語意接地**：心智圖的本職是讓 LLM 先建立**語境**再抽詞，所以 `Accessibility` 在此語料被接成 `可近可用性`（非通用 MT）、`對位`→positioning（非「音樂對位」）。稀有度/不能錯是**第二**位的篩選，疊在已正確接地的詞上。
- **glossary 語意接地貫穿（只補空、不覆寫）**：`_aggregate` 對對照表 gold 詞的 term/變體/類別/信心/來源**不動**，但**開放心智圖補它空的 gloss/names/關係**——讓第一原則落到對照表詞（解 `可近可用性` names 空、與 `Accessibility` 不連的問題）。合併語意一律「**舊有值就不動、只填空格**」，零覆寫風險、純擴增。
- **判準取代主觀「自創」**：termhood / 翻譯關鍵性（文獻），可操作；過收的 20% 交 **verified 閘門**攔（不靠提示詞硬調）。
- **tier**：T1/T1g 對照表 gold；T2 Pass2 收錄（is_special）；T3 純語意（只留心智圖、不進 CAG）。
- **整檔與大小守門**：build 不切 chunk；`KNOW_MAX_FILE_CHARS=6000`，超過整份旁路並進 audit，不截斷。P1/P2 輸出上限各為 12288 tokens。
- **多語**：每詞 `names:{lang3:譯法}`（§5.4），Pass1/Pass2 抽、跨檔只補空聯集；CAG 帶各語言固定譯法當判官前綴。
- **Qwen3.8-27B Q2_K_L 容錯**：輸出**逐物件解析** `\{[^{}]*\}`（**要求扁平、勿巢狀**，否則內層物件被拆走）；Pass2 **文本在前、指令在後**（避免回吐 prompt）；`_is_term`/`_clean_name` 守門擋整句/數字/填充語/整句譯法。
- 統計新詞（凝固度+左右熵）已停用（常用詞噪音）。對齊：GraphRAG、CAG 知識準備、Hybrid CAG-RAG（evict 最低分＝聲學/接地稽核 CAG）、termhood 抽取。
- **8 類實體 taxonomy ＋ 未分類**：人名／組織機構／地點地名／產品品牌／技術或概念術語／外來詞或英文詞／事件活動／作品或標題，外加 **未分類**（誠實兜底，取代舊「領域專名」catch-all）。`_canon_category` 把雜類併回 8 類；`_tune_categories` 對 misfit 重判；`relabel` 免重抽即可重正規化。
  > 設計沿革：曾試「演講/會議/翻譯/對話/溝通」場景軸（v2），實測 70% 塌進「演講」→ 改回 GraphRAG 標準實體軸（v3，最大類 54%、分佈較散、未分類僅 3.7%）。

### 5.3 增量可擴充（持續長知識庫，不動程式碼）

設計目標：**每場講者/每批新稿只要丟進 `sources/` 再跑一次 build，知識庫就增大**，既有成果不被覆寫、不重複抽。

| 機制 | 做法 | 效果 |
|---|---|---|
| **內容 hash 增量** | `.manifest.json` 存每檔 sha256；build 時只重抽 hash 改變的檔，未變者重用快取抽取結果 | 加新檔＝只抽新檔（驗證：第二次 reused=2 未重抽）；只快取 glossary/doc（昂貴），skip 不快取 |
| **遞迴掃描** | `os.walk` 掃 `sources/` 全子目錄 | 可 `sources/<講者>/` 分場整理而**不掉詞**（仍全量彙整）→ 兼顧 per-speaker 隔離 |
| **鎖定欄位不覆寫** | note frontmatter `locked:true` → 重建保留 term/category/gloss/variants/reason/names/term_lang | 人工核可過的詞不被下次 LLM 抽取蓋掉（對齊治理鐵律） |
| **全量彙整** | build 每次從**當前** sources/ 重新彙整 term/occ/關係 → 重生 vault/cag | 檔案在不在 sources/ 決定詞在不在庫；manifest 只決定要不要重跑 LLM。**故勿把用過的檔搬離 sources/，會掉詞** |

> 增量旗標：`POST /api/knowledge/build {incremental:true}`（預設）只重抽改動檔；`incremental:false` 全重抽（並把所有 doc 抽取寫進 manifest 初始化快取）。

### 5.4 多語抽取與擴充性（規格(一)；加語言＝LANGS 加一列、零 schema/prompt 改動）

採國際標準（DaMuEL 語言無關 KB 模式）。**已落實多語抽取**（非僅標注）：

- **語言註冊表** `langtag.LANGS`（單一事實來源）：規格(一) 全語系（國語/英/日/韓/泰/越/他加祿/印尼/德/法/西/葡/義/荷/捷/瑞典/盧森堡/斯洛伐克/波蘭/烏克蘭/俄/阿拉伯/土耳其/印地/泰米爾/烏爾都＝26 列）。
  每列 `{ISO-639-3: {script(ISO-15924), name, aliases}}`；「**廠商軟體同步更新增加語系**」＝加一列，**不動 schema、不動抽取程式**。
- **`names: {lang3: surface}`** 多語對照 map（**已取代寫死 en/ja**）；LLM 抽取時語言代碼『**直接平鋪當欄位**』（如 `"en":"…","ja":"…","ko":"…"`，保 JSON 扁平讓 `_iter_objs` 解得到），`_parse_names` 掃頂層任意語言鍵 → `canon_lang` 正規化（未知鍵丟、不臆測）→ `_clean_name` 按書寫系統守門（CJK/泰文限字數、拼音文字限字數+詞數）。
- **離線偵測** `engine/langtag.py`：純 Unicode 區塊規則（無模型、無上網，對齊 GlotScript），`langs_present`/`primary_lang`/`lang_tag`/`canon_lang`/`name_limits`；新增天城文(印地)、坦米爾偵測。
- **聲學 key 語言化**：`pinyin_key` 為 cmn 專屬；各語言各自 phonetic key（假名/metaphone…）並存——此為唯一隨「有 ASR 來源語言數」線性成長處（KB 其餘層 O(1)）。
- **抽取邊界**：KB 只**收錄來源裡出現的固定譯法錨點**（present 語言），不要求 Qwen3.8-27B Q2_K_L 翻譯成全語系；譯成第 N 種語言由翻譯模型 runtime fan-out（用錨點當約束）→ **KB 不隨顯示語言數膨脹**。
- 驗證（2026-06-23 build）：59 詞抽到多語（eng/jpn，∵語料僅中英日），如 二八法則→{eng: Pareto Principle, jpn: パレト原則}。語料含韓/泰時自動抓 kor/tha，零改 code。

### 5.5 Obsidian 心智圖後端（可瀏覽，不依賴 Obsidian 執行）

vault/ 一詞一 `.md`（frontmatter: term/term_lang/pinyin_key/category/source/tier/confidence/reason/is_special/locked/**names**/variants；
body: gloss + 多語 + `[[關係/同音/錯法/領域]]`）。`engine/vault_graph.py` 把 `[[..]]` 解析成 graph；前端 `/graph.html`（force-directed，過濾 source/category/min_conf，離線 vanilla canvas）。

- **API**：`GET /api/knowledge/graph`、`/api/knowledge/categories`、`/api/knowledge/sources`；`POST /api/knowledge/build`、`/api/knowledge/relabel`（免重抽：類別正規化，保留多語 names）、`/api/knowledge/decide`（逐檔抽取/排除覆寫）。
- **逐檔抽取決策**（`.decisions.json`，rel→extract|exclude）：自動判斷（乾淨副檔+非逐字檔名→extract）為預設，**使用者可逐檔在管理面板手動凌駕**；`extract` 可強收被自動排除的 .srt/.txt/逐字檔（`_extract_file(force=True)` 略過鐵律門），`exclude` 可排除乾淨檔。決策持久、build 時生效、不重抽既有快取。
- **節點詳情面板**顯示：gloss 語意、領域、來源（中文標籤＋CAG 收錄）、tier/信心、收錄判準 A/B/C、**多語固定譯法 names**、字音（term_lang+拼音）、ASR 變體。圖例移入瀏覽窗左下（節點顏色＝來源）。
- **工具**：`bin/oburl` 顯示 LAN 網址（`192.168.0.103:8080/graph.html`，SSH 進來開瀏覽器）。

### 5.6 聲學檢索：IPA 兩層級聯（§4.1 音韻通道；採 Apple 2409.15353 的 NPD 思路）

知識側產出 term（+variants）回灌聲學索引（`phonetic_index`），供 §4.1 按讀音檢索正解。
**IPA 是管線末端的專案級設施、正式採用**（規則式 NPD，純 CPU、零訓練、零安裝，符 MISA 天條）。**已接線（2026-06-23）**：

- 種子：`build`/`relabel` 寫 `data/knowledge/acoustic.json`（**只存原始字串、不烤 key/IPA**）。
  **聲學靶只收 CAG（is_special）**＝不能錯的專名/術語；T3 純語意一般詞不當靶（避免往無關詞過度修正）。
  逐字稿不一定 match 心智圖屬正常——心智圖只當生成的語意脈絡，**不參與「改不改」的聲學判定**。

- **兩層級聯查詢**（`phonetic_index.query`）：
  - **層1 離散 pinyin_key 完全比對**：key 相同即命中（最便宜；登錄過的變體 `紅雞→宏碁` 直接撈）。
  - **層2 IPA 音素距離**（`ipa.py`）：層1 未命中→把詞拆 **IPA 音素序列**算**加權編輯距離**，
    `距離 ≤ 0.34` 依近到遠補召回——**撈未登錄的音近詞**（紅雞 key≠宏碁 但 IPA 僅差送氣 tɕ/tɕʰ → 召回）。
    離散→連續，容錯更廣；混淆類（送氣/清濁、捲舌↔平舌、n/l、前後鼻音）算低代價。
  - 跨語言：IPA 是通用音標，規格(一) 26 語系共用同一距離函式（他語系另掛 G2P→IPA，距離碼語言無關）。

- **落地**：層2 IPA 索引一次性寫 `data/index/acoustic/ipa_index.json`（糾正側，§1.2；CPU 預算、供審查檢索/離線檢視）。
- **IPA 是「一次性 data」非執行瓶頸**：知識側 build 時預算（CAG 過 IPA 存索引）；查詢側 span 轉 IPA＝查靜態表（微秒級，非模型推論）。真正花時間的是 LLM 偵測/重生，與 IPA 無關。
- 現行 `ground_context` 把 IPA 聲學召回與 bge 局部語意組成兩軸證據，再交 Qwen 判定。
- **符全域正規化規範**：key 走 `phonetics.pinyin_key`、IPA 走 `ipa.ipa_tokens`、身分走 `variants.flatten`，零新正則。

> **三層級聯（含尾端 fallback）**：層1 拼音 key → 層2 IPA 距離 → **都不近＝此 span 不在 CAG＝不是已知不能錯詞 → 不硬改**，留人工/regex（避免無中生有）。
> 來源 1、2＝累積字典 `correct`、參考詞表 `files/refterms/`（前身 `files/ANE`，已更名；ANE 原指學習式嵌入，與本規則式 IPA 無關）。
> **更遠的學習式選項**（Apple ANE 聲學嵌入，entity recall 高 4%+）需訓練資料＋訓練模型，**與 MISA 天條衝突，不採用**；IPA 規則式即本專案終態。

---

# Part C — 系統規格

## 6. 節點上的資料（JSON schema）

### 來源容器：以「批次/場次」為單位

```jsonc
{
  "batch_id": "evt_2026q3_geology_01",
  "domain": "geology",
  "domain_prior": "本場為海岸地質演講，常見豆腐岩、海蝕平台…",   // 弱先驗
  "classification": "地質",                                       // 跑前手選注入（§3.6）
  "meta": { "meeting_date":"2026-07-01","origin":"YouTube 海岸地質講座","source_kind":"informal" },
  "raw": { "text":"…整份逐字稿…","kind":"transcript","filename":"talk.srt" },
  "segments": [Segment],                                          // 站1 切分 chunk
  "profile": { "noise":0.3,"rarity":0.7,"regime":"乾淨×專特","chunks":[…] }, // 站1.5 診斷
  "versions": [
    { "vid":"v0","from":"asr_raw","text":"...","frozen":true },   // 原稿唯讀
    { "vid":"v1","from":"stt","text":"..." }
  ],
  "spans": [SpanNode],
  "snapshots": [ /* 每輪前快照，供退回（§3.6） */ ]
}
```

### `SpanNode`（收斂節點核心，分流就看它）

```jsonc
{
  "span_id":"s_017","seg_id":"c_03",
  "category":"地質","review":"存疑","context":"沿海可見[豆腐巖]地形",
  "history":[
    { "iter":1,"value":"豆腐巖","by":"model" },
    { "iter":2,"value":"豆腐岩","by":"model" },
    { "iter":3,"value":"豆腐岩","by":"human" }   // 人工編輯不計 volatility
  ],
  "volatility":0.1,"is_proper_noun":true,
  "grounding":{ "checked":true,"pass":true,"url":"wiki://豆腐岩","rrf_score":0.92 },
  "state":"stable|volatile|stuck",
  "decision":{ "to":"auto|human|regex","correct":"豆腐岩" }
}
```

### `Segment` / `Classification` / `TermEntry`

```jsonc
{ "seg_id":"c_03","idx":3,"raw_text":"…","normalized":"…","review":"存疑","span_ids":["s_017"] }

{ "class_id":"cls_geology","name":"地質","prompt":"判斷詞是否為 地形/岩體/海岸地名…並給標準寫法。" }

{ "term_id":"t_doufuyan","correct":"豆腐岩","type":"term","term_lang":"cmn-Hant","ipa":"...",
  "names":{ "eng":"tofu rock" },"variants":["豆腐巖","豆腐沿"],"domains":["geology"],
  "occurrences":[{ "batch_id":"evt_...","wrong":"豆腐巖","confidence":0.97,"grounding":"wiki://豆腐岩" }],
  "tier":"T1","confidence":0.97,"updated_at":"..." }
```

> 知識側 term 與收斂側 TermEntry 共用 names/variants/term_lang 欄；前者由 build 抽、後者由 commit 彙整，匯流於字典。

## 7. API（節點皆可瀏覽/編輯/腳本）

```
〔收斂側〕
 GET|PUT   /api/batches/{id}/sources
 POST      /api/batches/{id}/materials      (簡報→抽詞)
 POST      /api/batches/{id}/generate       (跑 STT/TTT 產多版本)
 POST      /api/batches/{id}/diagnose       (站1.5 regime profiling)
 POST      /api/batches/{id}/converge       (收斂迴圈，含 ReASC 停止)
 GET       /api/batches/{id}/spans          (含 history/state)
 GET|PATCH /api/batches/{id}/spans/{sid}    (人工改判)
 POST      /api/batches/{id}/spans/{sid}/ground
 POST      /api/batches/{id}/commit         (分流結果併入字典)
 GET|PATCH /api/dictionary/terms
 POST      /api/dictionary/sync             (重生 IPA/regex/TTS)
〔知識側〕
 POST      /api/knowledge/build             (sources→vault+cag+acoustic.json；CAG 只由此改；增量/clear 旗標)
 POST      /api/knowledge/relabel           (免重抽：類別正規化，保留多語 names)
 GET       /api/knowledge/graph | /categories | /stats
 GET       /api/knowledge/sources           (列檔+抽取狀態+每檔抽取/排除決策)
 POST      /api/knowledge/decide            (逐檔 extract|exclude|auto 覆寫自動判斷)
 POST      /api/knowledge/upload            (上傳乾淨來源檔進 sources/，不自動 build)
```
> 知識側 `build`/`relabel` 另寫 `data/knowledge/acoustic.json`（正解+變體）→ §4.1 `phonetic_index` 第三通道消費（§5.6）。

> **併發優先**：節點以 `batch_id`/`span_id` 為粒度獨立儲存，不同 batch/span 可並行處理與編輯；
> 衝突採節點層級樂觀並發（last-write/合併），不鎖整批。單卡 GPU 工作排程序列化，但 API/任務層全程非同步併發。

## 8. 管線執行順序（含 GPU 佔用）

```
切分        逐字稿 → bge 對齊主題邊界 → core chunk（不重疊收斂單位）        [GPU: embedding]
接地(先)    心智圖 query 橋 → bge 檢索 wiki 大庫(hnswlib)+心智圖卡 → 語境錨點 [GPU: embedding]
粗修        雙偵測器(變體+NER) → CAG 讀音 IPA 召回 → 錨點高信心同音錯直接改   [GPU: NER；CPU: IPA]
精修        仍存疑 span → Qwen3.8-27B Q2_K_L 帶 語境錨點+CAG/心智圖 受限驗證(選/棄權,RPM)     [GPU: judge llama.cpp]
循環收斂    約束式重生 → 對齊 + 量分歧；ReASC N/M/K 高信心早停（可選深化）    [GPU: embedding]
分流        粗修/穩定+接地✅→auto ; ❌→regex ; 搖擺→human
彙整+回灌   去重/累積 variant/信心↑（只併 verified）→ 字典 → IPA/regex/TTS 回灌
```

**排序原則**：① **先接地後修**（retrieval-then-correct）——先用 wiki 大庫+心智圖建語境錨點，再由粗到細修；
② 便宜濾網（NER/IPA，CPU）先擋，貴的（Qwen3.8-27B Q2_K_L）最後、只對仍模糊 span；
③ 候選正解只取 CAG/心智圖（curated, verified-only 才升 gold），wiki 一律 candidate 只當語境。
**正式配置為五核心服務常駐**：Qwen、bge-m3、NER、SaT、API 同時運行。

## 9. 硬體配置（單卡 RTX 3090 Ti / 24GB, Ampere）

> **Ampere 無 FP8**（FP8 要 Ada/Hopper）。量化一律用 **Q2_K_L GGUF**，不走 FP8 路徑。

### Profile A — 離線收斂批跑（重吞吐）

| 角色 | 模型 | VRAM | 駐留 |
|------|------|------|------|
| 常駐 NER | Chinese NER (BERT-base) | ~0.5GB | 全程 |
| 常駐 G2P | g2pW | ~0.5GB | 全程 |
| 常駐 嵌入 | EMBEDDING_MODEL | ~1.0GB | 全程 |
| 階段1 | faster-whisper large-v3 **int8** | ~2–3GB | 生成載入 |
| 階段1 | 本地翻譯 (TTT) | ~2–4GB | 可與 STT 同駐 |
| 階段4 | 判官 Qwen3.8-27B Q2_K_L GGUF (llama.cpp) | 實測 16838MiB | 4 slots × 32K |

- **實測常駐**：Qwen 16838MiB + bge-m3 1432MiB；整卡約 18339/24564MiB，無 OOM。
- **實測並發**：4 個短 JSON 請求同時占用 slot 0–3，約 1.02 秒全數完成；實際長文速度以 prompt 長度為準。

### Profile B — 即時直播

STT large-v3 int8 常駐（~2–3GB，串流）+ hotword top-k 偏置（輕）。重的收斂/judge/wiki **不在即時做**，留到事後 Profile A，避免拉高延遲。單卡即時無虞。

### 隱私配置

> 「When Helpful Context Leaks」(2026) 證實上下文偏置會洩漏內容 → **wiki 一律用離線 dump**（中文維基標題+redirect+摘要），不打線上 API：不外洩會議內容、無延遲、利高吞吐。

## 10. 已定方向（拍板）

1. **TTT 路徑**：主跑中文、英文為輔（zh↔en 增多樣性）；多語走 **wiki 分割**（混語/外語專名交 wiki entity 邊界切分，非 TTT 來回）。
2. **原始 ASR 保留不覆寫**（`versions[].asr_raw, frozen:true`，唯讀）；保留才能 diff 出「錯→對+上下文」。
3. **收斂 N/M/K** 使用者實機自測；先當上色/提示閾值，逐輪成形，成熟後才自動早停。
4. **字典收錄使用者親自判斷**；系統只做高信心提案，不全自動入庫（治理鐵律）。
5. **併發優先**：節點層級樂觀並發；GPU 序列化、API/任務層併發。
6. **互動**：人工按一次跑一輪 → 生成新資料 → 檢視/修改 → 再按下一輪。
7. **知識側持續擴充**：丟檔進 sources/ 再 build 即增大知識庫；內容 hash 增量、鎖定欄位不覆寫（§5.3）。

> 仍待實測：N/M/K 實際值、TTT 改寫策略細節、各 STT 引擎吞吐基準。

---

## 附：參考文獻

**2026（最新對照）**
- Reliability-Aware Adaptive Self-Consistency (ReASC) — <https://arxiv.org/html/2601.02970v1>
- Know When You're Wrong: Aligning Confidence with Correctness — <https://arxiv.org/html/2603.06604>
- Confidence-Aware Self-Consistency for Efficient CoT — <https://arxiv.org/html/2603.08999v2>
- Contextual Biasing for LLM-Based ASR with Hotword Retrieval & RL — <https://arxiv.org/abs/2512.21828>
- CLAR: CIF-Localized Alignment for Retrieval-Augmented Contextual ASR — <https://arxiv.org/html/2603.25460>
- Deep Contextual Reasoning from Broad Descriptions for ASR (Speech-LLM) — <https://arxiv.org/html/2606.10838>
- When Helpful Context Leaks: Privacy Risks in Domain-Adapted ASR — <https://arxiv.org/html/2605.28211>
- Hallucination Detection and Mitigation in LLMs (survey) — <https://arxiv.org/pdf/2601.09929>
- Enhancing RAG with Entity Linking (ELERAG) — <https://arxiv.org/abs/2512.05967>
- Semantic Entropy Probes（§3.4 τ）— <https://arxiv.org/pdf/2406.15927>
- Beyond Semantic Entropy: Pairwise Semantic Similarity — <https://arxiv.org/pdf/2506.00245>
- Explicit Abstention Knobs for Predictable Reliability（risk–coverage）— <https://arxiv.org/html/2601.00138v1>
- SelectLLM: Calibrating LLMs for Selective Prediction — <https://openreview.net/forum?id=JJPAy8mvrQ>
- Trusted Uncertainty: Confidence Calibration & Risk-Controlled Refusal — <https://arxiv.org/pdf/2509.01455>
- Entropy Alone is Insufficient for Safe Selective Prediction — <https://arxiv.org/pdf/2603.21172>
- NoRefER: Referenceless Quality Metric for ASR（站1.5 診斷）— <https://arxiv.org/html/2306.12577>
- Ground Truth-Free WER Prediction — <https://link.springer.com/chapter/10.1007/978-3-032-07959-6_3>
- Adaptive Test-Time Compute via Training-Free Difficulty Proxies — <https://openreview.net/forum?id=ztGHhyicWs>

**知識側建構（Part B）**
- GraphRAG: KG Construction + Hybrid Retrieval（實體+關係+gleaning+社群）— <https://arxiv.org/html/2507.03226v3>
- Don't Do RAG: Cache-Augmented Generation（知識預載三階段）— <https://arxiv.org/html/2412.15605v2>
- Enhancing CAG with Adaptive Contextual Compression（Hybrid CAG-RAG / evict 最低分）— <https://arxiv.org/html/2505.08261v1>
- Efficient Terminology Integration for LLM Translation（termhood 術語抽取/多語）— <https://arxiv.org/pdf/2410.15690>
- DaMuEL: Multilingual Dataset for Entity Linking（每語言分存 names/aliases）— <https://arxiv.org/pdf/2306.09288>
- GlotScript: Writing System Identification（ISO-15924 離線偵測）— <https://arxiv.org/pdf/2309.13320>
- Apple: Phonetic Retrieval-Based Augmentation for ASR（聲學檢索三步法；simple replacement WER 4.84 > context-aware 4.44，證 IPA 不該直接換）— <https://arxiv.org/pdf/2409.15353>
- Revisiting ASR Error Correction with Specialized Models（abstain/copy-input 防 over-correction）— <https://arxiv.org/html/2405.15216v2>
- LLM GER for Rare Words with Phonetic Context（phonetic context 防過度修正）— <https://arxiv.org/html/2505.17410v1>
- 舊繁中模型研究: Traditional Chinese LLMs（3B/8B, Llama 3.2, function calling；受限驗證升級候選）— <https://arxiv.org/abs/2501.13921>

**基礎**
- BIO/IOB Tagging (Ramshaw & Marcus 1995; CoNLL-2003)；ckip 用 BIOES（E/S 結尾/單字）— <https://aclanthology.org/W03-0419.pdf>
- Generative Annotation for ASR Named Entity Correction (EMNLP 2025) — <https://aclanthology.org/2025.emnlp-main.1052/>
- Entity Linking with Wikidata: A Systematic Literature Review — <https://dl.acm.org/doi/10.1145/3795134>
- JudgeLM: Fine-tuned LLMs are Scalable Judges — <https://arxiv.org/pdf/2310.17631>
- Thinking Small Models are Efficient LLM Judges — <https://arxiv.org/html/2509.13332v1>
- g2pW: Conditional Weighted Softmax BERT for Polyphone Disambiguation — <https://arxiv.org/pdf/2203.10430>
- CLUENER2020: Fine-grained Chinese NER Benchmark — <https://arxiv.org/pdf/2001.04351>
- llama.cpp — <https://github.com/ggml-org/llama.cpp>
