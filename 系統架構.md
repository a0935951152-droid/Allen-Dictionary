# 系統架構

> 天條與治理鐵律見 [CLAUDE.md](CLAUDE.md)。資料結構見 [資料結構與資料庫.md](資料結構與資料庫.md)、端點見 [功能與API.md](功能與API.md)。

## 1. 核心理念

ASR 逐字稿的錯字有兩種失敗模式，對應兩個偵測器：

| 失敗模式 | 例 | 偵測器 |
|---|---|---|
| 已登錄的確定誤聽 | 宏碁→紅雞 | **詞表直掃**（`scan_terms`：CAG 變體簡繁直掃，不靠 NER） |
| 未登錄的音近錯字（漢字）| 生痕化石→生核化石 | **詞表直掃 + IPA 鄰域**（對 CAG 正解滑漢字窗，繞過 NER） |
| 未登錄的音近錯字（實體）| 龍山寺→龍山是 | **NER + IPA**（NER 圈專名 span、IPA 對 CAG 撈音近正解） |

全管線是 **retrieval-then-correct（先接地、兩軸並行判讀、再由判官拍板）**，採 Apple 2409.15353 召回→驗證骨架：
**便宜檢索（bge/hnswlib/IPA）早做且廣、貴的 LLM 判官晚做且窄、只對仍模糊的 span**。

> **2026-06-24 翻轉**：原為「CAG 小庫精修在前、接地兜底在後」；改為「**先接地後修**」——
> 在還不知整段在講什麼時就改字會改錯既成事實，故先用 wiki 大庫＋心智圖立**語境錨點**，再修。

NER＝**可錯的定位器**（圈出像專名的 span，可以錯），不是辨識器；認對交給 IPA 召回 + 錨點 + Qwen 判官。

**服務形態（2026-08-25 定調）**：qwen、tei-embed、ner、sat、api 同時常駐。
Qwen3.8-27B Q2_K_L 是唯一 LLM，build 與判官共用；4 slots×32K context。舊 LLM 與 reranker 已移除。前端按鈕經 **ops 佇列**（`data/ops/` 檔案佇列＋主機 `dic agent`）
觸發 build/judge 並輪詢進度——api 容器不碰 docker（封閉天條）。

## 2. 雙側資料流

```
純淨側（知識庫，答案來源）            糾正側（逐字稿，問題語境）
sources/ 乾淨文件                     batches/ 逐字稿
  │ Qwen 兩階段抽取（共用常駐服務）         │
  ▼                                    ▼ 切分(bge)
心智圖 vault + CAG ──────橋──────▶ 接地：心智圖實體當 query → wiki 大庫(hnswlib)
  │（verified 才回灌）                  │   = chunk 語境錨點（不改字）
  ▲                                    ▼ 偵測→兩軸接地→判官(四態)
  └──────────── 回灌 verified 詞 ◀──── 字典 ◀── 人工清單
```

- **純淨側給「答案候選」**（心智圖語意 / CAG 正確度），**糾正側給「問題語境」**（chunk 上下文），LLM 撮合。
- 「一份實體兩投影」：心智圖（語意，給接地檢索）⊃ CAG（正確度，給判官候選與聲學靶）。

## 3. 判定側管線（兩層之二）

```
匯入 ─▶ 切分 ─▶ ground_context(偵測＋兩軸接地) ─▶ 判官(Qwen 四態) ─▶ 人工清單 ─▶ 字典 ─▶ 回灌
```

1. **匯入**：STT/TTT 逐字稿（僅 .txt/.srt）→ `batch.raw` + v0；judge 智慧整理日期/來源。
2. **切分**（`chunk.py`）：bge 把 core 切點對齊主題轉折（相鄰句相似度低谷；缺服務則規則貪婪降級），
   core 連續鋪滿不重疊＝下游判定單位；`ctx_before/after` 重疊只供手動校正視圖。
3. **ground_context**（`review`+`reports`+`know_index`+`wiki_index`，**不改字**）：
   - **偵測**（`review.review_detect_segment`，CPU）：變體掃描 ＋ NER 定位器 → 對 CAG 過 **IPA 單層雙門檻** 撈音近正解。
   - **兩軸並行讀數**（`reports.build`）：**IPA命中軸**（聲學候選＋登錄變體必收；**IPA 含聲調**——真同音須
     含調全同，還是↔海蝕 d=0.15 非滿分）× **局部語意撐軸**（候選 vs 局部句，經心智圖實體→wiki 橋擴大語境，
     排名制前 k=5）。`rule_hit`＝兩軸皆撐。兩軸是**獨立量測**、非先後。
     **變體分級（A1）**：登錄變體若本身是通用詞（jieba 詞頻 ≥COMMON_FPM）或字典正解 → `ambiguous`——
     不進必收層、證據標「曾登錄誤聽樣態(需語境確認)」；不成詞字串（龍山是）維持確定誤聽必收。
   - **語境錨點**（`know_index.ground_context`）：每 chunk → 相關心智圖實體 → 預存 wiki 橋接 → 寫 `seg.ground_context`
     （`source=mind/wiki`，wiki 帶 `via`＝橋接實體）。wiki 一律 candidate、永不升 gold。
4. **判官**（`refine.judge_span`；`dic judge` 或前端「判官出動」＝以常駐 Qwen 批次判所有存疑）：
   吃兩軸證據＋整段接地錨點＋平行多語 → Qwen `{abandon,xling}` → **四態** = `rule_hit` × `abandon`：
   ①命中+不棄=修(auto)；②命中+棄/③不命中+棄=維持；④不命中+不棄(跨語救回)=人工。
   **原詞守門（A3 退化版）**：原詞為通用詞 → 即使判「修」也封頂第④態人工（永不 auto）。
   已判 human 的 span 重跑不再重擲骰（人工佇列不流失）。
5. **人工清單**（`refine` 端點）：第④態列人工裁決（`decision=human`，永不自動）。
6. **字典 + 回灌**（前端 ⑥「回灌/匯出」站）：commit 去重/累積 variant（**只併 decision=auto**；
   通用詞不進 variants、confidence 以 **distinct 批次數** 計）→ sync 重生三套：**regex**（安全子集，
   可盲替換）＋ **contextual**（通用詞/字典詞變體，下游不得盲替換）＋ IPA/TTS（容器待納入）。

## 4. 模組職責

**判定側**
| 模組 | 職責 |
|---|---|
| `chunk` | bge 邊界切 chunk（core 連續不重疊） |
| `review` | 偵測（三器）：詞表直掃(`scan_terms`) + NER 定位器 + IPA 召回；偵測前剝 ⚠️/(?…) 註記（純 CPU） |
| `phonetic_index` | 聲學檢索：**單層 IPA 雙門檻**（CAG 模糊 / 字典·refterms 真同音）；`variant_correct` 登錄變體必收；`scan_terms` 詞表直掃(繞過 NER) |
| `ipa` | **G2P 派發器→IPA**：漢字 pypinyin **TONE3 含聲調**（音節尾附 T1–T5 token，互換小代價 0.30；距離分母只算音段——同調對與無聲調時代逐位一致）／日文 pykakasi→espeak ja／其餘 espeak-ng；`word_distance`（漢字逐字取 max、其餘整詞）。單一入口，知識側建靶＋判定側算距離共用 |
| `lexicon` | **通用詞判定**（jieba 內建詞頻表查表，`COMMON_FPM` 門檻）：A1 變體分級／A3 判官封頂／A5 commit·sync 守門三處共用 |
| `reports` | **兩軸證據**：`acoustic`(IPA命中＋登錄變體必收＋ambiguous 加註) × `semantic_local`(局部語意，心智圖∪wiki anchor)；`rule_hit` |
| `refine` | 判官：Qwen 吃兩軸＋整段錨點＋多語 → `{abandon,xling}` → 四態；常用原詞封頂人工 |
| `variants` | 異體字攤平（岩/巖，0 成本前置）；`to_simp`（查簡體詞表用） |

**知識側（建置）**
| 模組 | 職責 |
|---|---|
| `knowledge` | sources → 兩階段 LLM 抽取 → vault 心智圖 + CAG + acoustic.json；CAG 變體 CRUD |
| `know_index` | 心智圖實體嵌入；接地時 chunk→心智圖檢索 + 預存 wiki 橋（`ground_context`） |
| `wiki_ingest` | zhwiki dump → `entries.jsonl`（標題+別名，OpenCC 轉繁） |
| `wiki_index` | wiki 嵌入 bge → hnswlib 近鄰（~1.5M，增量 add_items） |
| `vault_graph` | vault md → 心智圖 graph JSON（前端可視化） |
| `langtag` | 離線語言/書寫系統偵測（多語擴充） |
| `harvest` | 統計新詞發現（凝固度+左右熵，輔助） |

## 5. SOTA 對照（本架構＝組裝已驗證技術）

- **召回→驗證骨架**：Apple 2409.15353（聲學檢索三步；simple-replacement WER 4.84 > context-aware 4.44 → IPA 只召回、LLM 帶上下文拍板可棄權）。
- **兩軸並行**：Phonetic+Semantic+NLI（2506.16528）——音近提名、語意約束信心、判官拍板；語意不生成候選字。
- **防過修**：abstain/copy-input（2405.15216、2505.17410）、PMF-CEC RPM（信心<0.5 棄權）。
- **離線偏置**：Helpful Context Leaks → wiki/上下文一律離線（封閉天條）。
