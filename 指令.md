# 指令手冊

> 天條見 [CLAUDE.md](CLAUDE.md)、端點全表見 [功能與API.md](功能與API.md)。
> `B=evt_250068`（批次 id）；API 在 `:8080`。`bin/dic` 服務控制、`bin/oburl` 顯示 LAN 網址。

## 1. 服務控制（`bin/dic`；無參數進選單）

架構：**知識庫＋wiki 跑一輪當 DB → 收斂棧常開 → 判官 Gemma 該出動才拉（獨佔窗口）**。
LLM 不常駐；⚠ Breeze-7B(vllm)/tei-rerank 已淘汰不啟動（compose 定義保留僅供回退）。

```bash
# ── 日常（收斂棧常開）──
dic up        # 起收斂棧：tei-embed + ner + sat + api（唯一 GPU＝embed ~2G）；自動背景啟動 ops 代理
dic api       # 只重建 api（改 app/ 程式後最常用；不碰其他容器）
dic status    # 健康狀態（收斂棧/獨佔窗口/已淘汰 分組 + GPU + /health）
dic logs [服務]  # 跟看日誌（預設 api）
dic down      # 全下（含通道與 ops 代理）
# ── 其他 ──
dic cpu       # 只起免顯卡（ner + sat + api；嵌入接地降級）
dic tunnel    # 開 Cloudflare 公開通道（⚠ 零認證 API，見 審查.md #2）
dic agent     # 前景跑 ops 代理（通常不用手動：dic up 會自動背景啟動）
```
`llm=false` 為預期常態（LLM 不常駐）。**不要停 `gitlab-runner`**（非本專案）。

## 2. DB 建置一輪 / 判官出動（Gemma 獨佔窗口：清 GPU→載 Gemma→執行→自動換回收斂棧）

```bash
dic build     # 知識庫「從頭」全抽（清快取；增量建置用前端 graph.html 按鈕，或 dic build --inc）
dic wiki      # wiki 大庫建置：解析 zhwiki dump → bge 嵌入 → hnswlib（~1.5M，長跑；dump 沒換不用重跑）
dic mind      # 嵌入心智圖實體＋wiki 橋接（build 後跑一次；前端 build 完成會自動接跑）
dic judge     # 判官批次：判完「所有批次」的存疑 span（= 前端「判官出動」按鈕）
```
**前端等價（含進度條與成功檢查）**：graph.html「執行 build」（增量勾選）＋「嵌入心智圖＋wiki 橋接」；
index.html ④「判官出動」。皆走 ops 佇列：`POST /api/ops/{build|judge}` → 主機 `dic agent` 執行
→ 前端輪詢 `GET /api/ops/status` 畫進度至 ✅/❌（api 容器無 docker 權限＝封閉設計）。
等價手動指令：
```bash
# 知識庫 build（增量；clear=true 重整格式）
curl -fsS -X POST localhost:8080/api/knowledge/build -H 'Content-Type: application/json' -d '{"incremental":true}'
# wiki 大庫（解析→嵌入，容器內 CLI；嵌入 ~15-30 分）
docker exec dict-api python -m app.engine.wiki_ingest
docker exec dict-api python -m app.engine.wiki_index
# 心智圖嵌入（接地橋）
curl -fsS -X POST localhost:8080/api/knowledge/embed_mind
```
wiki dump 下載（主機，一次性）：`zhwiki-latest-{page,redirect,all-titles-in-ns0}.sql.gz` →
`data/wiki/raw/`（見 [資料結構與資料庫.md](資料結構與資料庫.md)）。

## 3. 跑一條校正管線（判定側：偵測＋兩軸接地→判官→人工）

```bash
B=evt_250068
curl -fsS -X POST localhost:8080/api/batches/$B/segment                 # 切 chunk（重切清舊 span）
curl -fsS -X POST localhost:8080/api/batches/$B/ground_context          # 偵測＋兩軸(IPA×語意)＋寫 seg.ground_context
dic judge                                                                # 判官（Gemma 獨佔窗口，判所有批次存疑；前端「判官出動」同）
#  /coarse_fix（單批直判）只在 Gemma 窗口已開著時可用（前端「本批直判」按鈕同）
curl -fsS -X POST localhost:8080/api/batches/$B/refine                  # 第④態人工清單
curl -fsS -X POST localhost:8080/api/batches/$B/commit                  # 併入字典（只併 decision=auto；通用詞不進 variants）
curl -fsS -X POST localhost:8080/api/dictionary/sync                    # 重生三套：regex(可盲替換)+contextual(不得盲替換)+ipa/tts(pending)
```
檢視：`GET /api/batches/$B/spans`、`/spans/{sid}`；前端 ⑥「回灌/匯出」站有 commit/sync 視覺化。
管線參數實際生效值：前端「⚙ 參數」站或 `GET /api/pipeline/params`（唯讀；調整走 .env → `dic api`）。

## 4. CAG 變體管理（清雜訊）

前端 `/cag.html`。指令版：
```bash
curl -fsS 'localhost:8080/api/cag/variants?only_special=true'           # 列（變體多的在前）
# 設變體（覆蓋；寫 vault md + locked，build 不沖）
curl -fsS -X PUT localhost:8080/api/cag/variants/有形 \
  -H 'Content-Type: application/json' -d '{"variants":[]}'              # 清掉髒變體
```

## 5. 健康 / 除錯

```bash
curl -fsS localhost:8080/health                # 服務線路（llm=false 為常態：LLM 不常駐）
curl -fsS localhost:8080/api/ops/status        # ops 佇列/獨佔窗口進度（agent 心跳＋step/phase）
curl -fsS localhost:8080/api/pipeline/params   # 管線參數實讀值＋公式＋聲學/心智圖統計（含 ambiguous 數）
curl -fsS localhost:8080/api/wiki/stats        # wiki 索引就緒/條目數
curl -fsS localhost:8080/api/knowledge/mind_stats  # 心智圖嵌入數
docker compose ps                              # 容器狀態
docker exec dict-api python -c "from app.engine import phonetic_index as P; print(P.stats())"  # IPA 索引
tail -f data/ops/agent.log                     # ops 代理日誌（前端按鈕背後的執行紀錄）
```
