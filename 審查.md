# 審查.md — 審查紀錄

> 真相源＝code；本檔為當時觀察，修掉後請更新狀態欄。
> 目錄：**[2026-06-30 安全/Bug 審查](#2026-06-30-安全bug-審查)**（路徑穿越/零認證/參數斷線）｜
> **[2026-07-02 結構審查](#2026-07-02-結構審查常見詞誤修根因)**（常見詞誤修根因鏈 R1–R8＋演算法定性＋修法細節）。

---

# 2026-06-30 安全/Bug 審查

> 全專案審查（注入面 + 邏輯 bug）。範圍：`services/api/app` 全部、compose、schemas。

## 乾淨面（已確認無風險）
- **無命令注入**：全專案無 `subprocess`/`os.system`/`os.popen`/`shell=True`。
- **無 SQL**：JSON-backed 儲存，無 DB 查詢拼接。
- **無危險反序列化**：無 `eval`/`exec`/`pickle`/`yaml.load`/`marshal`。
- vault 端檔名防護到位：`_safe()`（`knowledge.py:894` 把 `/ \ : * ? " < > |`＋空白換 `_`）、
  `move_source` `normpath+startswith`（`knowledge.py:108`）、`save_upload` 用 `basename`＋過濾 `..`（`knowledge.py:1259`）。
- `term_id`/`cid` 只當單檔字典的 dict key，不落檔名 → 無穿越。
- LLM 輸出解析 `refine.py:85` 有 try/except 包覆，扁平 JSON 穩。

---

## 🔴 #1 路徑穿越 → 任意 `.json` 覆寫（中度；開 tunnel 後升高）  狀態：未修
- **位置**：`schemas.py:108` `BatchNode.batch_id: str`（零驗證）→ `storage.py:48 _batch_path` →
  `os.path.join(batches_dir, f"{batch_id}.json")`。
- **入口**：`POST /api/batches`（`main.py:52`）`batch_id` 來自 **JSON body**，不受路徑單段限制，可含 `/`、`..`。
- **影響**：`{"batch_id":"../correct/dictionary"}` → 覆寫 `data/correct/dictionary.json`（內容為攻擊者可控
  BatchNode dump）→ `load_dict()` 後 pydantic 驗證失敗 → 資料毀損 / DoS。`classifications.json` 同理。
- **修法**：`batch_id` 加 `Field(pattern=r"^[A-Za-z0-9._-]{1,80}$")`；或 `_batch_path` 套 `_safe()`＋斷言
  結果仍在 `batches_dir` 內。`create_batch` 一併過濾。

## 🟠 #2 全端點零認證 ＋ 可一鍵公網開通（中度）  狀態：設計使然，待防護
- 所有端點無認證（離線 LAN 設計，符合天條 2）。但 `docker-compose.yml:204` cloudflared tunnel，
  `make tunnel` 無 token 時走 **TryCloudflare 隨機公網網址**，把整個無認證 API（含 DELETE batch、
  knowledge/build、檔案上傳、#1 穿越寫入）曝露公網。
- 現況：`CLOUDFLARE_TUNNEL_TOKEN` 空、profile 預設不啟動 → 目前未開，但「一條指令全開」。
- **建議**：開通道前套 Cloudflare Access / 基本認證；README/CLAUDE.md 標注風險。先修 #1。

## 🟡 #3 參數外移「三層斷線」（正確性/維運，非漏洞）  狀態：未接通
- `config.py` 新增 29 個調參（②③④⑤組），但：
  (a) 引擎 code 仍讀 module 常數（`phonetic_index.py:33 _IPA_FUZZY`、`reports.py:21 _LOCAL_FLOOR`、
      `ipa.py:51 _SUB_CONFUSE`、`phonetics.py:35 _HOMO_THRESHOLD`、`refine.py:24 JUDGE_CONCURRENCY=4`…），
      **未讀 `settings.*`**；
  (b) `docker-compose.yml:176` api `environment:` 只傳 ⑥ 收斂組（CONVERGE_*/RRF_K/GROUNDING_THRESHOLD）；
  (c) `.env`/`.env.example` 無這些鍵。
- → 調這些參數**靜默無效**。實際被 code 讀的只有 ①服務座標(7) + ⑥收斂(5) = 12/41。
- **修法**：引擎常數改 `from app.config import settings; settings.xxx`；compose 補傳 env；.env.example 補鍵。

## 次要
- `main.py:74` `delete_batch` 內冗餘 `import os`。
- query int 無上界（`segment.max_chars`、`wiki/search.k`、`knowledge/graph.limit`）→ 自架輕度 DoS。
- `refine.py:107` seg 缺失回 `"keep"` 未動 span（僅計數語意，無害）。

---

## 後續方向（使用者指定）
- **後期管線（gemma 判斷 → 人工修正，§4.1 階段②③）需繼續精修**。本檔安全項與精修可並行，
  但改字邏輯（`refine.judge_span` 四態）動到時，務必同步確認 #3 的閾值來源（避免調了沒生效）。

---

# 2026-07-02 結構審查（常見詞誤修根因）

> 觸發：`還是`/`等等` 類高頻詞被提名滿分、域內句 `rule_hit=True`，清 CAG 髒變體後問題仍在。
> 範圍：偵測（`review.py`/`phonetic_index.py`）→ 規則證據（`reports.py`/`know_index.py`/`ipa.py`）→
> 判官（`refine.py`）→ 回灌（`main.py` commit/sync）全鏈；對照實際批次資料 evt_250068 / evt_331072。
> 待辦落地見 [TODO.md](TODO.md) A 段（A1–A8，含完工順序與驗收基準）。

## ⛔ 操作凍結

**A1＋A2 落地前，不要對 evt_250068 / evt_331072 跑 `dic judge` 或 `commit`。**
兩批現存大量「還是→海蝕」存疑 span（250068 有 23 個、331072 有 18+，多個 `rule_hit=True`）。
四態下 gemma 一次 hit＋not-abandon → auto 修 → `commit`（`main.py:293`）把「還是」寫進
`海蝕.variants` → `/api/dictionary/sync`（`main.py:349`）吐無條件 regex `還是→海蝕` 全域替換。
一次失手＝永久性全域替換規則。

## 演算法定性（先說結論）

本系統＝**帶接地先驗的噪聲通道模型（noisy channel with grounded prior）**：

```
修正 ĉ = argmax   P_IPA(表面字 | c) × P_語意(c | 語境)
         c ∈ 候選集
```

- 通道項 P_IPA ＝ 聲學提名（`phonetic_index`，IPA 距離）；先驗項 P_語意 ＝ 局部句→心智圖→wiki
  兩跳接地（`know_index`/`reports.semantic_local`）。「不是相似就改」＝式子裡多了先驗項，方向正確。
- 講者詞→wiki 的**兩跳橋**（髒逐字稿不直查 wiki）是設計裡最關鍵的抗噪一步，維持。
- **但式子缺兩個成分、實作有兩條捷徑**，四者疊加＝常見詞誤修：
  1. 候選集缺 **null hypothesis**（c＝原詞自己）：`P(還是|還是)×P(還是|語境)` 從未被算，
     整個「維持原詞」假設外包給 gemma 一個 `abandon` bit。對不成詞字串（龍山是）null 先驗≈0 無害；
     對高頻詞（還是）null 先驗壓倒性 → 誤修來源。
  2. 先驗是**主題級**不是**詞槽級**：回答「這句在講海蝕的主題嗎」，不是「這個位置放海蝕通不通」。
     單域文件裡主題先驗處處飽和 → 閘門常開。
  3. 通道項有兩條「滿分捷徑」：登錄變體無條件 score=1.0（R1）＋無聲調 IPA d=0 真同音（R2）。
  4. 回灌把語境條件丟掉（R6）：驗證到「這類語境下 X→C」，儲存成無條件 (X→C)＋全域 regex，
     且 confidence 隨 span 數自我強化（正回饋＝發散，非收斂）。

## 失效鏈 R1–R8（按嚴重度）

### R1 「登錄變體＝確定誤聽」是無條件層 🔴
- `vault/terms/海蝕.md` `variants:[海石,海飼,還是,海是,海獅]`（源：觀光署錯誤樣態表）。
  對照表記錄的是「**某句中**曾誤聽」＝條件事實；`phonetic_index.variant_correct`（`:222`）＋
  `reports.acoustic`（`reports.py:52`）升級成「**任何語境**都是誤聽」——score=1.0、
  標「變體(確定誤聽)」、不受任何門檻（天條 4 被過度解讀）。
- 後果：`scan_variants` 每 chunk 直掃 → 每個「還是」都成 span（兩批 23＋18+ 個）。
  `龍山是` 類不成詞字串適用此機制；`還是` 類高頻詞先驗相反，不適用。

### R2 無聲調 IPA → 還是↔海蝕 d=0「真同音」 🔴（清資料治標無效的原因）
- `ipa.py:91` `Style.NORMAL` 無聲調：還(hái)↔海(hǎi)、是(shì)↔蝕(shí) 只差聲調 →
  `word_distance=0`。**就算手清變體，`scan_terms`（`phonetic_index.py:246`）滑窗 d=0 ≤ 0.28
  照樣重新提名、score=1−d/thr 照樣滿分**（`reports.py:59`）。
- 輸入是文字非音頻，每字聲調查表確定；誤聽對（講是 jiǎngshì↔薑石 jiāngshí）聲調常不同＝真訊號。
  當初去聲調保召回的顧慮，用「聲調小代價」而非「聲調硬過濾」即可兼顧（見修法 A2）。

### R3 原詞從未被接地（null hypothesis 缺席） 🔴
- 唯一防線＝`refine.py:76` gemma `abandon` 單 bit；一批 23 個同 surface span＝23 次獨立擲骰，
  任一次 hit＋not-abandon 即 auto。糾正側無任何頻率先驗（`harvest._STOP` 只用在知識側抽詞）。

### R4 排名制在單域批次退化為常開閘 🟠
- `know_index.top_terms` k=5、卡池 198 張單域 → 域內句 top-5 幾乎恆含候選。
- 實證：evt_250068 講**花枝丸**的句子撐 海蝕 0.46；evt_331072「瓊麻**還是**龍舌蘭」
  「海巡艦艇**還是**海關艦艇」（明顯連接詞）rule_hit=True，語意撐 0.44–0.67。
- 排名制當初解「bge 中文基線 0.35–0.47 絕對門檻無鑑別力」是對的；但「文件本身就在講 X」時
  「這句與 X 相關」推不出「這個詞是 X 的誤聽」。

### R5 commit/sync 把語境判斷壓扁成無條件替換（災難放大器） 🟠
- `main.py:293`：auto 修的原詞 append 進 `entry.variants`。
- `main.py:349`：sync 把**所有** variants 吐成無條件 regex 全域替換。
- `main.py:305`：confidence=0.5+0.1×len(occurrences) 以 **span 數**計——一批 8 個誤修 → 0.99
  「高信心」，頻率被誤當獨立佐證。
- 現況 `data/correct/dictionary.json`（283 條）尚乾淨；凍結令即為守住這點。

### R6 原稿「錯字⚠️正解」註記污染語意證據 🟠
- `review.py:24` `_DETECT_MARKERS` 只剝 ⚠️ 符號與 `(?…)`，**正解詞留在句內**：
  「還是⚠️海蝕」→「還是海蝕」→ 局部句字面含「海蝕」→ cos 虛高（0.67 那句即是）。
  現有 rule_hits 統計被灌水。
- 反面價值：這些註記＝**現成 gold 評測集**，目前被浪費還反向污染（見修法 A6）。
  另抽查發現至少一條註記本身疑似標錯（「瓊麻還是⚠️海蝕龍舌蘭」明顯是連接詞）→ gold 需人掃一遍。

### R7 span 無 offset、同 surface 每 chunk 只判第一次 🟡
- `scan_terms` 回傳 `start`，`_mk_span`（`review.py:82`）丟棄；`seen_surface` 去重 →
  一 chunk 5 個「還是」只建 1 個 span；`local_sentence`（`reports.py:41`）取**第一個**含詞句 →
  判的句子可能不是有問題的那個出現位置，修字也無法只改該處。常見詞下此設計不成立。

### R8 judge 證據措辭偏置 🟡
- `refine.py:73` 證據行「海蝕(**變體(確定誤聽)**)」＋「候選**正解**」＝提示詞先寫答案。
  判官應驗證規則層結論，不是複誦它。

## 修法細節（每項對應式子的哪一塊；順序 A1+A2 → A5 → A6 → A3/A4 → A7/A8）

### A1 變體分級：ambiguous 永不 auto〔修 P_IPA 被硬設 1〕
- **通用詞判定**（任一命中→ambiguous）：(a) 離線詞頻表 zipf≥5（每百萬詞≥10 次；詞頻檔一次放
  `data/files/`，符封閉建構）；(b) 變體本身是字典 correct 或 wiki 詞條；(c) 可選：**建庫期**
  gemma 一次性標註寫入 frontmatter（建庫期用 LLM 不違 MISA，runtime 仍純規則）。
- **存放**：vault frontmatter 加 `variants_ambiguous:`；`knowledge._write_acoustic` 帶進
  acoustic.json；`phonetic_index._build` 不把 ambiguous 放進 `variant_map` 直掃必收層。
- **runtime**：ambiguous 仍可由 IPA 模糊層提名（召回不丟），證據改「曾登錄誤聽樣態」不置頂；
  span 帶 `cap=human` → 四態算出 fix 也降級第④態人工。
- **驗收**：兩批「還是」span 零 score=1.0 變體證據；龍山是→龍山寺 recovered 不減。

### A2 聲調入距：真同音層只收含調全同〔修通道太寬〕
- `lazy_pinyin` 改 `Style.TONE3`，每音節 IPA tokens 後附聲調 token（T1–T5）；`_sub_cost`
  聲調互換代價 ~0.3。效果：還↔海 d=0.1、是↔蝕 d=0.15 → `word_distance(還是,海蝕)=0.15` →
  真同音層（門檻 0）：**掉出免費滿分**；CAG 模糊層（0.28）：0.15<0.28 **仍提名**
  （召回不退），但 score≈0.46 非 1.0，語意/判官有裁量空間。
- ⚠ **實作坑（非改一個 enum 就好）**：`Style.TONE3` 拼音尾帶數字（`hai2`），但 `ipa.py`
  的 `_split`/`_WHOLE`/`_FINALS` 全預期無數字音節，直接餵 `hai2` 會解析失敗、退逐字元 fallback。
  正確作法：`_han_tokens`（`ipa.py:91`）**先剝尾部聲調數字 → 用現有邏輯處理 base 音節 →
  再 append 聲調 token（T1–T5）**。輕聲（無數字/5）給獨立 token。改完先單測 `word_distance` 再動 `_CONFUSE`。
- ⚠ **正規化坑（07-03 debug 發現）**：聲調 token 若進分母，同調詞對「分母+1、分子+0」全體變近
  （是↔寺 0.67→0.45）＝0.28 模糊層被隱性放寬、召回暴增。**分母只算音段 token**：同調對距離與
  改動前逐位一致（是↔寺 0.67 不變）、異調對只變遠——「召回面不擴大」由構造保證，0.28 免重測。
  另：聲調 token 判定用明確集合 `_TONES`（勿用前綴 `T`——espeak 缺席時逐字元 fallback 會吐字面 `'T'`）；
  跨書寫系統比對時聲調 token 增刪也算小代價（espeak 側無聲調，不能當整顆音素懲罰）。
- **驗收**：`word_distance(還是,海蝕)>0`；evt_250068 八個既有 auto 正修
  （三川殿/藻井/鎮瀾宮/鄉勇/對場作/燈節/刈包/三川店）全保留。

### A3 原詞對稱接地：rule_hit 改 margin 制〔補 null hypothesis〕
- **務實版（先做）**：原詞成立度 S_o＝zipf 頻率先驗＋原詞是否自身在 anchor/字典/wiki。
  分級：`zipf(原詞)≥5 → 永不 auto 封頂人工`；中頻 → 候選語意分數須高出 margin 才 hit。
  函數詞在任何 bge 相似度下都不該 auto 改——這是 margin 制的退化形式，一行邏輯擋整類錯誤。
- **升級版（A6 之後）**：詞槽替換測試——候選代入原句重嵌，量 Δcos（對候選卡）改善顯著才 hit；
  把先驗從主題級推向詞槽級。參數用 gold 集擬合，不手拍。

### A4 排名制加基線對比〔修先驗飽和〕
- `top_terms` 本已對全卡池算 cos，分佈免費：取該句對全卡池中位數 m、離散度 s，
  撐住條件改「rank≤k **且** (score−m)≥δ（或 z≥2）」。
  花枝丸句 海蝕 0.46 vs 基線 ~0.4x → z 小 → 拒；海蝕句 0.67 → z 大 → 收。δ 用 gold 集擬合。
- 這是「排名制取代絕對門檻」的正確一般化，非走回絕對門檻。

### A5 commit/sync 守門〔修回灌丟條件〕
- `commit`：append variants 前過 `is_common_word()`（與 A1 共用，抽成 `engine/lexicon.py`）；
  common 不進 variants，只記 occurrence（帶 `conditional:true` 留審計）。
  confidence 改以 **distinct batch 數** 計。
- `sync`：regex 只吐「非 common、非字典詞」變體；其餘輸出 `contextual` 清單，明示下游不得盲替換。
- 原則：判定期 per-context 的結論，儲存期不得壓扁成無條件規則。

### A6 ⚠️ 註記完整剝離＋轉 gold 評測集〔讓 A3/A4 可量測〕
- 註記文法左邊界未標（「還是⚠️海蝕前面」分不出正解邊界）→ **保守解析**：右側取與左側等長窗
  ＋IPA 音近檢核，配得上自動收、配不上進人工清單；總量幾十對，人掃一遍便宜（且需要，見 R6）。
- 落地 `data/gold/evt_xxx.json`（wrong/right/offset/seg）；偵測/嵌入文本剝乾淨（raw 不動）。
- 之後任何門檻調整（δ、margin、聲調代價）都跑同一套指標：偵測召回／修正精確／常見詞誤修率（目標 0）。

### A7 span 帶 offset、逐出現判定
- SpanNode 加 `start/end`；`_mk_span` 接住 `scan_terms` 的 start；取消 `seen_surface` 去重
  （每出現各建 span）；`local_sentence` 用 offset 取所在句。常見詞語境逐次不同，共用第一句判定不成立。

### A8 judge 證據中性化＋餵 `seg.ground_context`
- ambiguous 變體證據改「曾登錄誤聽樣態(需語境確認)」；「候選正解」→「候選詞」。
- prompt 加整塊語境錨點 `seg.ground_context`（木構造句可見「本段零海蝕訊號」）；
  明示「原詞為常用詞且該句通順時優先 abandon」。風險低（改措辭＋加輸入）。

## 執行規劃（分階段依賴與交付門檻）

> A1–A8 不是平面清單，有依賴與「可解凍」節點。核心洞察：**A1 單做會被 R2 繞過、A2 單做仍留無條件
> 變體層**，必須綁一起；**A5 沒到位前不得解凍**（gemma 失手仍會經 commit/sync 變全域 regex）。
> 貫穿原則：判定期 per-context 的結論，儲存期不得壓扁成無條件規則；新增閾值一律走 `settings.*`（見末節 #3）。

| 階段 | 內容 | 解什麼 | 前置 | 交付後安全狀態 |
|---|---|---|---|---|
| **P0** | 操作凍結 | 守住乾淨 `dictionary.json`（283 條） | — | 已生效 |
| **P1** | A1＋A2 | 斬兩條滿分捷徑（R1×R2 根治：清資料沒用的元凶） | P0 | 高頻詞不再滿分提名 |
| **P2** | A5 | 斷 commit/sync 放大器 | P1（`is_common_word` 共用 `engine/lexicon.py`） | **可正式解凍** |
| **P3** | A6 | 剝乾淨註記＋建 gold 評測集 | 可與 P1/P2 並行 | 之後調參可量測 |
| **P4** | A3＋A4 | 原詞對稱接地（補 null hypothesis）＋去先驗飽和 | P3（gold 擬合 δ/margin，不手拍） | 誤修率趨 0 |
| **P5** | A7＋A8 | span offset 逐出現＋judge 去偏置 | P1 | 收尾 |

- **P1 為何綁死 A1+A2**：A1 拿掉「無條件變體必收」，但 R2 的無聲調 d=0 會讓 `scan_terms` 滑窗把
  「還是」重新提名成滿分候選——只做 A1，手清＋分級都會被這條免費滿分捷徑繞過。反之只做 A2，
  登錄變體層仍無條件 score=1.0。兩者是同一個「通道太寬」的兩個洞。
- **P2 為何是解凍門檻**：做完 P1，gemma 偶爾失手仍可能 auto 修；但只要 commit/sync 守門（common 詞
  不進 `entry.variants`、regex 排除 ambiguous、confidence 改 distinct-batch），失手就進不了無條件全域規則。
  **P1＋P2 到位＝凍結令可正式解除**；在此之前 evt_250068/331072 一律不跑 `dic judge`/`commit`。
- **P3 先於 P4**：A3/A4 的 δ、margin、聲調代價都要用資料擬合，沒有 gold 集就只能手拍——所以先建集再調參。
- **最小驗證切片**（想先探風險再全做）：只落 A2 的 `word_distance`，驗「還是↔海蝕 d>0」且 evt_250068
  八個既有 auto 正修（三川殿/藻井/鎮瀾宮/鄉勇/對場作/燈節/刈包/三川店）不掉，再往上疊 A1。

## 驗收基準（A 段完工定義）
- **誤修率**：兩批所有高頻詞零 auto 修，只允許第④態人工建議。
- **召回不退**：evt_250068 八個 auto 正修全保留；真變體 recovered（龍山是類）不減。
- **評測集**：gold pair 落地，取代單批肉眼驗證。

## 與 06-30 審查的交叉
- A 段會新增多個閾值（zipf 門檻、聲調代價、δ/margin）——落地時一律走 `settings.*`，
  勿再擴大 **#3 參數三層斷線** 的斷線面（上節）。
- #1 路徑穿越修好前勿開 tunnel（#2），與本節凍結令並行有效。
