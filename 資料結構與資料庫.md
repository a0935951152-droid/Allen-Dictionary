# 資料結構與資料庫

> 天條/鐵律見 [CLAUDE.md](CLAUDE.md)、流程見 [系統架構.md](系統架構.md)、端點見 [功能與API.md](功能與API.md)。

## 索引
- [1. 節點模型（`schemas.py`）](#1-節點模型schemaspy)
- [2. 目錄樹（`data/`）](#2-目錄樹data)
- [3. 純淨側落地檔](#3-純淨側落地檔)
- [4. 糾正側落地檔](#4-糾正側落地檔)

---

## 1. 節點模型（`schemas.py`）

階層：`BatchNode ▸ Segment ▸ SpanNode`。

### BatchNode（批次/場次容器）
`batch_id, domain, domain_prior, classification, meta{meeting_date,produced_date,origin,source_kind},
raw{text,kind,filename}, sources, segments[], versions[], spans[], snapshots[]`
（`snapshots[]`＝ground_context 跑前 spans 快照）

### Segment（切分後 chunk＝判定單位）
| 欄位 | 說明 |
|---|---|
| `seg_id, idx` | `c_000`…、序號 |
| `raw_text` | ＝v0；去標點 core，下游判定單位 |
| `start,end,core_text` | core 在全文 offset、含標點原文（手動劃分） |
| `ctx_before,ctx_after` | 重疊上下文，**只供顯示、不進判定** |
| `ref, langs{}` | 同段 en/ja 對照、26 語平行版本（多通道一致性） |
| `ground_context[]` | **接地語境錨點**（不改字）；`{source:mind/wiki, …}`（見下） |
| `span_ids[], review` | 所屬 span、chunk 判定 |

`ground_context` 卡：
- mind：`{source:"mind", term, gloss, category, variants[], is_special, score}`
- wiki：`{source:"wiki", via:<橋接實體>, title, aliases[], score}`

### SpanNode（判定節點核心）
| 欄位 | 說明 |
|---|---|
| `span_id, seg_id, category` | id、所屬 chunk、NER 類別 |
| `history[]` | `{iter,value,by:model/human}`；**[0]＝ASR 原詞、[-1]＝current 建議正解** |
| `review` | 正確/存疑/錯字 |
| `is_proper_noun` | 是否專名 |
| `grounding` | `{checked, pass, url, candidates[]}`（讀音召回候選） |
| `reports` | **兩軸證據** `{acoustic[], semantic[]}`（IPA命中 × 局部語意撐，各帶 candidate/score/evidence） |
| `rule_hit` | 兩軸皆撐（IPA命中 且 局部語意撐）＝判官判斷前的規則閘 |
| `decision` | `{to:pending/auto/human/regex, correct, regex}`；**commit 讀 `decision.correct`** |
| `refined` | 判官（coarse_fix）已判定落地 |

> 語義鏈：偵測產 span（history=[原詞,建議]、current=建議、review=存疑）→ ground_context 算兩軸 `reports`/`rule_hit` →
> 判官拍板：確認改 `decision.to=auto, correct=候選`；棄權維持 `review=正確, decision.to=pending`；第④態 `decision.to=human`。

---

## 2. 目錄樹（`data/`）

```
data/
├ batches/<id>.json            糾正側：逐字稿批次（BatchNode）
├ correct/                     ── 糾正側輸出 ──
│  ├ dictionary.json           累積字典（TermEntry：correct/variants/confidence；同時是聲學索引第三源＝閉環）
│  ├ sync.json                 sync 三套輸出落檔（regex/contextual/ipa/tts；給下游）
│  └ classifications.json      分類注入定義
├ ops/                         ── 前端↔主機 ops 佇列（常駐 Qwen）──
│  ├ request.json / request.run   前端排入的 build/judge 請求（agent 認領後改名）
│  ├ status.json               進度回寫（state/step/phase/log；前端輪詢畫進度條）
│  └ agent.{alive,pid,log}     dic agent 心跳 / pid / 執行日誌
├ knowledge/                   ── 純淨側（知識庫）──
│  ├ sources/                  乾淨原始文件（_excluded/ 封存被排除的逐字/字幕）
│  ├ vault/{terms,domains}/    Obsidian 心智圖 md
│  ├ cag/<domain>.md           CAG 詞表（人類可讀）
│  ├ acoustic.json             聲學種子（CAG 正解+變體 → 餵 IPA 索引）
│  └ .manifest.json/.decisions.json   增量 hash / 逐檔抽取決策
├ index/                       ── 糾正側（檢索庫）──
│  ├ acoustic/ipa_index.json   IPA 聲學索引（落地）
│  ├ wiki/{hnsw.bin,ids.jsonl,meta.json}   wiki 大庫向量索引
│  └ know/cards.json           心智圖嵌入卡（接地檢索）
├ wiki/                        wiki 大庫原料
│  ├ raw/                      zhwiki dump（page/redirect/titles .gz）
│  ├ entries.jsonl             解析後 {title,aliases,pageid}（~1.5M）
│  └ manifest.json             dump 來源 + hash（增量）
└ files/refterms/             參考正解詞表（領域包，進聲學真同音層）
```

---

## 3. 純淨側落地檔

### vault term md（`knowledge/vault/terms/<term>.md`）
frontmatter：`term, term_lang, pinyin_key, category, source, tier, confidence, reason,
is_special, locked, names{ISO:寫法}, variants[]`；body：`*gloss*` + `[[關係/同音/錯法/領域]]`。
> `locked:true`＝人工核可 → 知識庫重建保留語意欄位（治理鐵律 6）。

### acoustic.json（聲學種子）
```jsonc
{ "count":274, "variant_count":312,
  "terms":[ {"correct":"宏碁","variants":["紅雞","宏基"],"is_special":true,"tier":"T1"} ] }
```
> `is_special`(CAG)→IPA 模糊層；其餘→真同音層。`_write_acoustic` 從 vault variants 產出。

### cag/<domain>.md
CAG 詞表的人類可讀版（canonical / variants / 兩面向）。

---

## 4. 糾正側落地檔

### index/acoustic/ipa_index.json（單層 IPA）
```jsonc
{ "count":502, "fuzzy":0.28, "exact":0.0,
  "terms":[ {"correct":"宏碁","ipa":["x","ʊ","ŋ","tɕʰ","i"],"cag":true} ] }
```
- `cag:true` → IPA 逐字距離 ≤`fuzzy`(0.28) 模糊召回；其餘 ≤`exact`(≈0) 真同音。
- 逐字距離 `ipa.word_distance`＝各字 IPA 距離取**最大值**（抗共享前綴稀釋：豆腐沿↔豆腐腦判遠）。

### index/wiki/（wiki 大庫向量索引）
- `hnsw.bin`：hnswlib cosine 圖（dim 1024 bge-m3，~1.5M 點，增量 add_items）
- `ids.jsonl`：label → `{title,aliases}`（與圖 label 對齊）
- `meta.json`：`{n,dim,space,model,fields}`
> wiki 一律 candidate（鐵律 5）。建置：`wiki_ingest`→`wiki_index`（CLI 或端點）。

### index/know/cards.json（心智圖嵌入）
`{cards:[{term,gloss,category,variants,names,is_special,hash,vec}]}`；接地時 chunk 餘弦比向量找相關實體。
增量：term 內容 hash，只嵌新/變動。

### dictionary.json（累積字典，閉環產物）
`{term_id:{correct,variants[],wrong,confidence,…}}`；只併 `decision=auto` 的 verified 修正。
